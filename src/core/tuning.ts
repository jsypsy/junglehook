/**
 * 게임 감각을 좌우하는 상수 전부. 밸런싱은 이 파일만 만진다.
 * 값의 근거는 docs/PROGRESS.md의 계측 기록에 남긴다.
 */
export const TUNING = {
  /** 중력 (px/s²) */
  gravity: 2000,
  /** 자유 비행 공기 저항 (1/s) — 발사 직후 속도를 읽을 수 있는 범위로 빨리 내린다 (B4 0.12 → B5 0.2) */
  airDrag: 0.2,
  /** 로프에 매달린 동안의 저항 (1/s) — 0이면 진폭이 안 죽어 "수직 정지" 상태가 생기지 않는다 */
  ropeDrag: 0,
  /** 로프가 닿는 최대 거리 (px) — 타깃 후보의 반경. 340은 앵커선(175)+340=515(화면 69%) 아래가
   *  "잡을 것 없는 확정 낙하"라 사용자가 "하단 30%에서 로프를 못 쏘고 죽는다"고 느꼈다 → 450 (D-007) */
  reach: 450,
  /** 홀드 중 로프가 감기는 속도 (px/s) — 고도 회복 수단. 0이면 순수 진자 */
  reelSpeed: 110,
  /** 감기 시 접선 속력 증가 지수 — 0: 속력 유지, 1: 각운동량 보존 (r_old/r_new 배) */
  reelGain: 0,
  /** 스윙 펌프 (px/s²) — 매달린 동안 진행 방향으로 넣는 가속. 오래 매달릴수록 높이 차오르는 축 */
  swingPump: 200,
  /** 펌프가 붙는 속력 상한 (px/s) — 90° 진폭 속도 √(2·g·minRope)≈720 아래로 두면 회전·느슨해짐 없음.
   *  (350/1100은 360° 회전이 났고 각속도 2.5회전/s로 앵커가 안 보임 — 실플레이 피드백) */
  swingMaxSpeed: 1050,
  /** 홀드 중 로프를 강체(막대)로 — 90° 위에서 느슨해지지 않아 펌프로 천천히 한 바퀴를 넘길 수 있다 */
  rigidRope: true,
  /** 로프 최소 길이 (px) — 이 이하로는 감기지 않는다. 짧을수록 각속도가 빨라 정신없다 (90→130: 0.6회전/s) */
  minRope: 130,
  /** 설계 기준 뷰포트 (px) — frame.html 실측값 */
  viewW: 393,
  viewH: 749,
  /** 이 아래로 떨어지면 사망 (px) — 화면(749) 아래로 공이 완전히 사라지는 지점. 사망 연출(파편·흔들림)이
   *  데드라인을 대신 보여준다. 강물 띠는 줌 변화로 위아래 움직여 버그처럼 보여 제거 (D-007) */
  killY: 770,
  /** 시작 위치·속도 — 첫 탭 직후 자유 비행으로 던져진다 */
  startPos: { x: 60, y: 300 },
  startVel: { x: 380, y: -120 },
  /** 카메라 줌아웃 — 속도 speedLo에서 1.0, speedHi에서 min까지 (빠를수록 앞을 더 보여준다) */
  camZoom: { speedLo: 600, speedHi: 1200, min: 0.78 },
  /** 이어하기(리워드 광고) 판당 최대 횟수 — D-001 "1~2회 제한" */
  maxContinues: 2,
  /** 이어하기 재출발: 마지막으로 지나친 앵커 기준 오프셋과 초기 속도 — 정상 릴리스(+30°, 전방 위)와 같은 궤적 */
  continueSpawn: { dx: 75, dy: 130, vx: 520, vy: -300 },
  /** 계절 진행 (D-010): stepM마다 봄→여름→가을→겨울, 경계에서 blendM 동안 색 보간 */
  season: { stepM: 250, blendM: 40 },
  /** 소닉 파워 (D-012): 한 앵커에서 loopsToArm바퀴 돌면 장착, 놓으면 로켓처럼 직선 대시 */
  sonic: {
    loopsToArm: 3,
    /** 대시 거리 (m) — 250은 계절 한 구간을 통째로 건너뛰어 너무 쉬웠다 → 150 (BUILD 18). 벽괴물 이후 찬스를 잡기
     *  어려워져 보상을 키움 → 200 = 73일 (BUILD 22, 사용자 "찬스로 나아가는 일수를 늘리자") */
    dashMeters: 200,
    /** 대시 속도 (px/s) — 200m를 4초에 */
    dashSpeed: 2500,
    /** 대시 중 순항 고도 (px) — 앞부분에서 여기로 떠오른다 */
    cruiseY: 230,
    /** 대시가 끝날 때 이어지는 일반 비행 속도 — 다음 잎을 잡을 시간을 준다 */
    exitVel: { x: 650, y: -260 },
    /** 타이밍 게이지: 마커가 왕복하는 주기(Hz)와 성공 구간 반폭(0~0.5). 구간 밖에서 놓으면 충전 소멸 */
    gaugeHz: 0.7,
    /** 성공 구간 반폭 — 0.08(114ms)은 거의 다 됐다. 찬스가 확정 지급되는 대신 여기로 난이도를 옮김 → 0.06(86ms, BUILD 23) */
    sweetHalf: 0.06,
    /** 찬스가 한 번 주어지는 구간 (m) — BUILD 27, 사용자 "슈퍼 대시를 1년에 1회". 1년 = 사계절 한 바퀴 = 1000m.
     *  계절마다(250m) 한 번은 대시(200m)가 다음 계절을 바로 물어 "잡기 몇 번 → 회전 → 대시" 사슬이 됐고,
     *  봇 계측에서 거리의 77%가 대시였다 (2026-08-31) */
    chanceStepM: 1000,
    /** 찬스 발동 (D-015): chanceStepM마다 한 번, 그 구간에서 **내가 잡는 k번째 앵커**에서 터진다.
     *  k는 시드+계절로 정한 [chanceGrabMin, chanceGrabMax] 랜덤. 같은 앵커를 다시 잡는 건 안 센다(낚시 무의미).
     *  위치가 정해진 잎을 "잡아야" 하던 방식(B15~22)은 잡히느냐가 운이었고 벽괴물 이후 뒤돌아가 잡을 수도 없어졌다.
     *  [2,6]은 매 계절 시작 직후에 터져 "잡기 몇 번→회전→대시→다음 계절" 사슬이 됐다 → [3,10]으로 계절 안에 흩뿌림.
     *  BUILD 27에서 단계가 1년(4배)이 되며 재조정: 봇 계측 1년당 잡기 95~135회(2026-08-31) → [12,50]이면
     *  찬스가 그 해의 앞~중반에 흩어지고, 남은 절반은 순수 스윙 구간이 된다 */
    chanceGrabMin: 12,
    chanceGrabMax: 50,
    /** 게이지 성공 구간 중심 — 찬스마다 시드 랜덤 [sweetFrom, sweetTo] (BUILD 24, 사용자 "항상 고정 위치면 더 쉽다").
     *  양 끝 근처는 마커가 방향을 바꾸며 두 번 스쳐 난이도가 달라지므로 0.2~0.8로 제한 */
    sweetFrom: 0.2,
    sweetTo: 0.8,
    /** 입력 유예 (s): 놓기 직전 이 시간 안에 마커가 성공 구간에 있었으면 성공. 화면 마커는 마지막 프레임(≤16ms 전)이고
     *  터치 지연(iOS 50~80ms)이 더해져 판정 시점 마커는 보이던 자리보다 0.07~0.13칸 앞서 있었다 — "안쪽인데 실패" (BUILD 26) */
    inputGraceSec: 0.09,
    /** 슈퍼 매뉴얼 카드를 보여주는 횟수 — "다시 안 보기"를 누르기 전까진 항상 (BUILD 26, 사용자: 2회 뒤 데모가 안 보여 혼란) */
    manualShows: 1e9,
    /** 찬스 앵커에 로프가 걸린 순간 멈추는 시간 (s) — "소닉 찬스" 알림 */
    freezeSec: 0.45,
  },
  /** 뒤쫓는 위협 (BUILD 19, 난이도 계측 결론): 간격·사거리 노브는 "높게 놓고 불확실하면 매달리는" 안전 전략을
   *  못 죽인다(120초 0/10 사망). 세션 30~60초(D-001)를 만드는 건 시간 압박 — 뒤에서 쫓아오는 폭풍이
   *  매달려 기다리는 시간을 비용으로 만든다. 속도는 1차 램프 + 사계절 주기 2차 램프(anchor와 같은 문법) */
  threat: {
    enabled: true,
    /** 시작 시 플레이어보다 이만큼 뒤에서 출발 (px) — 첫 스윙 두세 번은 화면에 안 보인다 */
    headStartPx: 600,
    /** 속도 (px/s): 시작 → rampX 지점에서 최대. 안전 봇(35°)의 순항 8m/s=400px/s를 rampX 부근에서 넘어선다 */
    speedMin: 300,
    speedMax: 420,
    /** 2차 램프: 사계절 한 바퀴(1000m)마다 +speedPerCycle, 상한 speedCap */
    speedPerCycle: 40,
    speedCap: 560,
    /** 고무줄: 플레이어보다 이 이상 뒤처지지 않는다 (px) — 빨리 가도 여유가 무한히 쌓이지 않아 압박이 유지된다.
     *  1000px = 300px/s 기준 3.3초 = 스윙 한 주기(≈2초) + 여유. 봇 계측(PROGRESS 2026-08-30)에서 760~1200은
     *  생존 시간 차이가 거의 없었다 — 죽음은 3~4초 넘는 스톨(초근접 잎 잡고 제자리 회전)에서 난다 */
    maxLeadPx: 1000,
    /** 찬스 잎에 걸리면 폭풍이 앵커 기준 이만큼 뒤로 물러나 멈춘다 (px) — 3바퀴 충전(≈7.5s)이 고무줄(3.3s)보다 길어
     *  실기기에서 회전 중에 잡혔다(사용자 피드백, BUILD 21). 회전 반경(로프 ≤ ~300)보다 넉넉히 뒤 */
    chanceBackPx: 450,
  },
  /** 거리 → 미터 환산 (px per m) */
  pxPerMeter: 50,
  /** 타깃 선택: 플레이어 기준 선호 지점 오프셋 (전방·위쪽 바이어스) */
  targetBias: { x: 130, y: -150 },
  /** 앵커는 플레이어보다 최소 이만큼 위에 있어야 후보가 된다 (px) */
  targetMinAbove: 20,
  /** 이만큼 뒤로 지나간 앵커는 후보에서 제외 (px) — 낮게 날 때 "앞은 너무 높고 뒤는 제외"가 겹치지 않게 120 (D-007) */
  targetBehindLimit: 120,
  /** 앵커 배치 — 난이도 곡선 */
  anchor: {
    /** 첫 앵커 위치 */
    first: { x: 210, y: 170 },
    /** 세로 배치 중심과 허용 범위 */
    baseY: 175,
    yMin: 80,
    yMax: 360,
    /** 가로 간격: 시작 → rampX 지점에서 최대 */
    gapMin: 175,
    gapMax: 300,
    /** 세로 흔들림: 시작 → rampX 지점에서 최대 */
    jitterMin: 35,
    jitterMax: 130,
    /** 1차 램프가 최대에 도달하는 x (px) — 9000은 180m(≈15s)에 최대 난이도라 세션이 목표(30~60s)의 절반 (D-006) */
    rampX: 18000,
    /** 2차 램프 (BUILD 18, 사용자 "4계절이 지날수록 점점 어려워지게"): rampX 뒤로 사계절 한 바퀴(season.stepM×4)
     *  지날 때마다 간격 +gapPerCycle·흔들림 +jitterPerCycle — 상한까지. 거리에 비례하므로 계절 경계에서 뚝 끊기지 않는다.
     *  간격 상한은 reach(450)보다 넉넉히 아래 — 잎이 물리적으로 닿을 수 없는 배치는 "내 탓인 죽음"이 아니다 (D-001) */
    gapPerCycle: 45,
    jitterPerCycle: 25,
    gapCap: 390,
    jitterCap: 175,
  },
} as const

/**
 * A/B 프리셋 — `?p=b4`처럼 URL로 고른다 (실기기에서 번갈아 판정용). 기본은 TUNING 그대로(B5).
 * 프리셋은 TUNING 위에 덮어쓰므로 게임 시작 전에 한 번만 적용한다.
 */
export const PRESETS: Record<string, Partial<Record<keyof typeof TUNING, unknown>>> = {
  /** BUILD 4: 회전 없음(≤83°), 느긋한 스윙 */
  b4: { rigidRope: false, swingPump: 150, swingMaxSpeed: 650, airDrag: 0.12 },
  /** BUILD 18 난이도 실험(`?p=hard`): 앵커가 더 빨리·더 넓게 벌어진다 (rampX 18000→11000, gapMax 300→330,
   *  jitterMax 130→165). "너무 쉽다" 피드백의 A/B용 — 기본값은 아직 그대로 */
  hard: { anchor: { ...TUNING.anchor, rampX: 11000, gapMax: 330, jitterMax: 165 } },
  /** BUILD 19 A/B: 위협 없음 (BUILD 18과 동일한 규칙) */
  nothreat: { threat: { ...TUNING.threat, enabled: false } },
  /** BUILD 19 A/B: 더 빠른 위협 */
  chase2: { threat: { ...TUNING.threat, speedMin: 340, speedMax: 480, speedPerCycle: 50, speedCap: 620, maxLeadPx: 800 } },
}

export function applyPreset(name: string | null): string | null {
  if (!name || !(name in PRESETS)) return null
  Object.assign(TUNING as Record<string, unknown>, PRESETS[name])
  return name
}

