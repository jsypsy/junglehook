# 작업 일지

## 2026-08-30 — 프로젝트 시작

- 컨셉 확정: 원버튼 그래플 스윙, 수익(리워드 광고) 1순위 (D-001)
- 이름 확정: 정글훅/junglehook — 충돌 검색 완료 (D-002)
- 스캐폴딩: 슈팅스타에서 이식 (D-003). platform/·storage·sound·analytics·tools·
  플레이북. 접두어 jgh.v1/jgh_, BUILD=1, 플레이스홀더 main.ts
- 검증: npm test / npm run build 통과 확인 후 첫 커밋

## 2026-08-30 — Phase 0 프로토타입 구현

- core: physics(진자+구속 투영) / anchors(시드 난이도 곡선) / game(상태머신·타깃 선택)
- 계측 주도 튜닝 3회전 (sim-observe 패턴, 슈팅스타에서 이식):
  1. reach 270: 낮은 비행에서 타깃 전무 → 1.9s 사망 → reach 340
  2. 고도 회복 수단 부재: 매 사이클 침하 → 3.4s 사망 → reel-in 110px/s 추가
  3. 속도 무한 증가(700px/s+) → airDrag 0.12/s 추가
  → 최종: 봇 30s 생존 · 314m · 속도 350~720 안정 (D-005)
- render/input/main: 트레일·타깃 링·HUD·사망 오버레이·베스트 저장(jgh.v1)
- Chromium 실렌더 확인: 타이틀→스윙→비행→사망→신기록 갱신 정상
- 남음: **사람 실플레이 판정** (Phase 0의 성공 기준) — 로컬에서 npm run dev

## 2026-08-30 — 실플레이 1차 피드백 → 물리 재작업 (BUILD 2)

- 실플레이: 156m → 206m (≈15초). 피드백: "너무 빨리 죽는다", "수직으로 매달려 못 움직이는
  케이스", "붕붕 돌수록 점프력이 세지면 좋겠다 — 물리가 핵심 재미"
- 계측 1: 사람 흉내 봇 30시드 — rampX 9000 중앙 21s/237m, 18000 33s/377m, 30000 54s/609m
- 계측 2: 무한 홀드 → 첫 스윙 54° 뒤 12초 만에 정지. 로프 저항 0으로 해도 24°까지 감쇠
  → 저항이 아니라 **적분기 오차 누적**(스텝당 -½g²dt²). 순수 진자 60°→2°/15s
- 계측 3: 정지 상태 탈출 — 놓고 0~300ms 안에 재누름이면 다음 앵커 잡힘, 600ms면 사망
- 수정: 에너지 보존 투영(physics.stepBody) / 감기 분리(reelIn) / 스윙 펌프(pump) /
  ropeDrag·reelGain·swingPump·swingMaxSpeed 튜닝 노브 / rampX 18000 / BUILD 2
- 계측 4: pump×cap×gain 12조합 — gain 0·pump 350·cap 1100 확정 (D-006)
- 테스트 28개 통과 (물리 3개 추가: 무감쇠·감기·펌프 상한)
- 개발 훅: `import.meta.env.DEV`에서 `window.__jgh`(game·meters·selectTarget) 노출 —
  크롬 MCP 자동 플레이용. 크롬 탭이 hidden이면 rAF가 멈춰 게임이 안 돌아감 (창을 앞으로)
- 남음: BUILD 2 실플레이 판정 — 쉬워졌으면 gapMax/jitterMax↑
- 피드백(BUILD 2 플레이 중): "돌다 뒤로 가면 거리가 줄어야 하는데 그대로" → distancePx를
  하이워터마크에서 **현재 위치 기준**(pos.x − startPos.x)으로. 점수는 죽은 지점 기준 = 과회전의 비용.
  BUILD 3
- 피드백(BUILD 3): "360° 회전이 원작 스펙? 너무 빨라 앵커가 안 보임" → 6조합 계측,
  minRope 130·pump 150·cap 650 확정(회전 없음, 최대 83°, 각속도 1/4). BUILD 4
- BUILD 5 "충전기": 강체 로프(rigidRope)·투영 배율 클램프·pump 200/cap 1050/airDrag 0.2·
  카메라 속도 줌아웃·`?p=b4` 프리셋 스위치(타이틀 우하단 `B5·b4`로 표시). 계측: 무한홀드 4.2s에
  첫 회전, 봇 거리 중앙 881m/60s. 테스트 31개
- 피드백: "어느 정도 내려가면 앵커가 있어도 못 잡는다" → reach 340의 결과(앵커선+340≈515 아래는
  잡을 것 없음, 사망선 830까지 '확정 낙하' 구간이 화면 40%). 정책 유지 여부·가시화는 판정 후
- BUILD 6: 어두운 강물(killY 수면, 830→760)·사거리 밖 앵커 흐림 (D-007). 렌더러 drawWater.
  크롬 탭 hidden이라 픽셀 검증 불가(rAF 0회) — 실화면 확인은 사용자
- 피드백: "강물이 위아래로 움직여 버그 같다 / 게임오버 비주얼 살리자 / 하단에서 로프 못 쏨 /
  시작 사용법 데모 넣자(한줄팡처럼)" → BUILD 7: 강물 제거·killY 770·사망 연출(파편·충격파·흔들림·
  카운트업)·reach 450·behindLimit 120·시작 화면 루프 데모·문구 "꾹 누르면 매달리고, 떼면 날아가요"
- 검수 도구: hidden 탭에서 rAF가 멈춰도 `__jgh.stepOnce(dt)`로 틱을 구동하고 캔버스를
  scratchpad 수신 서버(POST)로 저장해 확인하는 방식 확립 (스크린샷 도구는 hidden 탭에서 타임아웃)
- 시작 데모를 3단계로 확장(4.6s 루프): 누르면 매달림 → 떼면 비행(다음 앵커에 링) → 다시 누르면
  다음 앵커에 매달려 스윙 → 페이드. 캡처로 3단계 확인
- 데모 다듬기: 첫 스윙을 진자 코사인 궤적(바닥 최고속)으로, 정점 전 +35°에서 릴리스 → 접선 방향
  베지어 비행 → 잡는 순간 로프 스냅(90ms)+링 확산 → 관성 이어받는 ease-out 스윙
- 폰(iOS 사파리) 하단 검은 영역 → 캔버스를 CSS 100%가 아니라 visualViewport 실측 px로 고정
  (position:fixed, visualViewport resize 구독). 아이폰 핫스팟이 IPv6 전용이면 맥이 192.0.0.2를
  받아 폰에서 접속 불가 → IPv6 글로벌 주소 `http://[…]:5173/`로 접속
- 데모 비행 도착 접선을 전방으로(앵커2 0.78w, 제어점 80px, 잡는 각 −55°) — "정점 지나 뒤로
  떨어지며 잡히는" 모양 제거. 게임오버 제목 "추락!" → "놓쳤다!"
- 클로드 디자인 캔버스로 비주얼 방향 확정: 밝은 카툰 정글, 토큰 1장 + 화면 4장 (D-008).
  사본을 `design/`에 저장. 게임오버 문구 "끝"으로(렌더러 반영)
- 앱인토스 테스트 배포 1회: `npm run ait:build` → `npm run ait:deploy` (BUILD 7, 번들 77.7KB/gzip 24.7KB).
  deploymentId 01a05196-…, 기록 tools/latest-deployment.json. 콘솔 MCP는 이 세션에 미등록이라 CLI 경로 사용
- 앵커 표현 확정: 구름은 배경, 앵커는 덩굴 매듭 (D-008)

## 2026-08-30 — BUILD 8 카툰 렌더러

- renderer.ts 전면 재작성 (D-008 설계도): 하늘 그라데이션·태양·구름(패럴랙스 0.15, 주기 반복)·
  3겹 숲 물결(0.3/0.5/0.75)·앞쪽 잎, 덩굴+매듭 앵커(사거리 밖 α0.5), 외곽선 로프, 얼굴 있는
  플레이어(눈이 진행 방향으로 쏠림, 홀드 중 가늘어짐), 칩형 HUD(거리 흰 칩·최고 노란 별 칩),
  시작 화면(외곽선 제목·부제 칩·데모 카드·CTA 칩), 결과("끝" 별 배지·카드·최고/신기록 칩·CTA)
- 배율 분리: 월드 `s`(줌 포함) / 장식·HUD·카드 `u`(줌 무관)
- 버그: 칩 높이 계산이 폰트 굵기(900)를 크기로 읽어 흰 기둥 → px 정규식으로 수정
- index.html 배경·theme-color #bfe8f5, apps-in-toss primaryColor #ff7f3f
- 캡처 검증: 시작 2단계·스윙·고속·사망 3단계

## 2026-08-30 — BUILD 9 이어하기 광고 루프 · 하루/계절 캔버스

- 하루·계절·날씨 캔버스(D-010): 장면 9장 + 타임라인. 2차 점검 반영(새벽 halo). 앵커를 덩굴 고리로
- BUILD 9: core `continueRun`/`continuesLeft`(maxContinues 2, continueSpawn), 입력에 좌표 전달,
  결과 카드에 "광고 보고 이어하기 n/2" + "다시 하기" 버튼(히트테스트), 광고 흐름(adBusy), Analytics
  연결(game_start·deep_play·game_over·ad_reward). 테스트 32개

## 2026-08-30 — BUILD 10 사계절 배경

- core/season.ts: 거리→계절(250m 주기, 경계 40m 보간 비율, 진행도). render/color.ts: oklab 보간
- 렌더러: 계절 팔레트(하늘·해·구름·숲 3겹·앞잎·덩굴) 보간, 겨울 눈(결정론 패턴, 갈수록 거세짐),
  계절 진입 칩, 앵커를 덩굴 고리로. 시간대·비는 보류(D-010 구현 결정)
- 테스트 37개 (season 3·color 2 추가). 캡처: 봄/경계/여름/가을/겨울(눈)

## 2026-08-30 — BUILD 11 캐노피 배경 · 잎 앵커 · 계절 태양

- 잎 앵커, 봄/여름 대비(파스텔+벚꽃잎 vs 진한 파랑), 태양 표정 4종·여름 광선
- 정글 배경 2안 캔버스 → A 캐노피 채택(D-011). 렌더러: 캐노피 2층+기둥+땅+안개+양치식물. 60프레임
  평균 0.67ms(맥 크롬) — 성능 여유
- 계절별 게임 규칙 변화 3안 제안(보류)
- BUILD 12→13: 봄 꽃잎 제거, 가을 낙엽(큼직), 눈송이 확대, 태양 크기 여름↑겨울↓. 50m 테스트 주기로 로컬
  판정 후 250m/40m 복귀. 배포

## 2026-08-30 — BUILD 14 소닉 파워

- core: SonicState(회전각 누적·바퀴·장착·대시), releaseInput에서 대시 시작, updateDash(순항 고도·거리 소진·탈출 속도).
  테스트: 3바퀴 장착 → 대시 → 250m 뒤 일반 비행 (38개)
- 렌더: 충전 링·장착 오라·스파크, 대시 잔상·불꽃·속도선, 결과 카드 소닉 횟수. 계측 sonic 횟수
- 캡처: 7.5초 홀드에 3바퀴 장착 → 대시 → 254m 지점에서 일반 비행·타깃 링 정상
- 타이밍 게이지(sonicMarker/sonicInSweet, gaugeHz 0.7·sweetHalf 0.08) + 플래시 모션 블러. 테스트 38개
- BUILD 15: 찬스 앵커(계절마다 1개, 시드 랜덤, 금색 후광), 히트스톱 알림, 찬스에서만 충전, 슈퍼 모드
  외형(파랑·1.5배·선글라스·번개). 테스트 39개
- BUILD 16: 슈퍼 모드 색을 따뜻한 계열로(백열 몸·빨강→주황→노랑 불꽃·흰/주황/빨강 번개). 배포
- BUILD 17: 찬스 잡는 순간 "SUPER!" 별 배지, 결과 카드 "슈퍼 ×N". 배포. (hidden 탭 캡처가 시간 초과를
  반복해 이번 배지는 실기기 확인으로 대체)


## 2026-08-30 — BUILD 18 슈퍼 대시 150m · 계절 표정

- 사용자: "난이도가 너무 쉽다 — 일단 슈퍼 아이템은 150m로". `sonic.dashMeters` 250→150 (계절 한 구간을
  통째로 건너뛰던 것을 60%로). 난이도 A/B용 `?p=hard` 프리셋 추가(rampX 11000·gapMax 330·jitterMax 165) — 기본값은 아직 그대로
- 주인공 얼굴이 계절을 탄다 (팔레트에 player/playerHi 추가, 표정은 지배 계절 기준·색만 보간):
  봄 온화한 노랑+볼 홍조+미소 / 여름 붉음+八자 눈썹+헥헥 입+양옆 흘러내리는 땀방울 / 가을 주황+미소 /
  겨울 하양파랑+파란 볼+八자 눈썹+덜덜 물결 입+이목구비 잔떨림(몸은 안 떤다 — 조작감 보호).
  슈퍼 모드(선글라스)는 계절 무관. 잔상·사망 파편도 계절 몸색
- 크롬 검수: `__jgh.game` 순간이동 + 중력/펌프 0으로 매달린 정지 상태 캡처 — 봄·여름·겨울 확인.
  (가을은 숲 색이 로프 색과 겹쳐 자동 크롭 실패, 코드 경로는 봄과 동일)
- 사용자 "4계절이 지날수록 점점 어려워지게": `anchors.difficultyAt` — 1차 램프(rampX 360m) 뒤로 사계절 한 바퀴(1000m)마다
  간격 +45·흔들림 +25, 상한 390/175 (상한×1.15 < reach 450 — 닿을 수 없는 배치는 안 만든다). 테스트 40개.
  "어렵다"의 정의: 놓아도 되는 타이밍 창의 폭 — 간격·흔들림이 그 창을 좁힌다(사거리는 억울함 비용이 커 안 건드림)


## 2026-08-30 — 난이도 계측 (BUILD 18, 봇 시뮬레이션)

사용자 "너무 쉽다, 슈퍼 대시 때문인가" → 고정 규칙 봇(매달렸다 각도 θ 넘고 상승 중이면 놓기, 자유비행 중엔 항상 누름)으로 계측.
시드 8~10개, 120fps, 슈퍼는 loopsToArm=999로 끔.

- **슈퍼 대시는 원인 아님**: 슈퍼 끈 25° 봇 60초 860m(사망 0/8) vs 슈퍼 쓰는 봇 817m — 3바퀴 대기(≈7.5s)가
  150m 이득을 상쇄해 거리상 거의 중립
- **진짜 원인 = 안전 전략의 존재**: 35° 이상에서 놓는 보수적 봇은 간격 420·흔들림 175·rampX 9000·사거리 360까지
  **모든 조합에서 120초 동안 0/10 사망**(8m/s로 전진). 죽는 건 25° 이하로 빨리·낮게 가는 봇뿐. 간격·사거리 노브는
  "빠른 플레이어"만 죽이고 "높게 놓고 불확실하면 매달리는" 플레이어는 못 죽인다. 65° 봇은 한 잎에 102초 매달려도 안 죽음
- 25° 봇 사망 시점(기본 rampX 18000): gapMax 300 → 114s(1/10), 340 → 93s, 380 → 66~73s, 420 → 39~49s.
  jitter 175는 각각 10~20% 앞당김
- 결론: 세션 30~60초(D-001)를 만들려면 **시간 압박(강제 결정) 메커닉**이 필요 — 후보: 뒤쫓는 위협(계절 돌수록 빨라짐),
  잎이 N초 뒤 찢어짐(단, 보수 봇은 스스로 놓으므로 단독으론 부족), 계절별 사거리 축소(D-007 억울함 비용)


## 2026-08-30 — BUILD 19 뒤쫓는 폭풍 (D-013)

- 계측 결론("시간 압박 메커닉 필요")을 받아 뒤쫓는 위협 구현. core: `threatX`·`cause`·`threatSpeedAt`·`threatGap`,
  `updateThreat`(전진·고무줄·잡힘), freeze 중 정지, 이어하기 시 재배치. 테스트 47개(위협 7 추가). 슈퍼 테스트 2개는
  찬스 잎에 7.5초 매달려 위협에 잡히므로 그 테스트에서만 `threat.enabled=false`
- 봇 계측 파일을 저장소에 남김: `src/core/sim-threat.test.ts` — `SIM=1 npx vitest run src/core/sim-threat.test.ts
  --disable-console-intercept`. 결과는 D-013. 지난 세션 봇과 달리 "정점 뒤 누름"이라 무위협 θ=25°가 13.9m/s로
  더 빠르다(지난 봇 8m/s) — 절대값보다 위협 유무의 차이를 본다
- 렌더: `drawThreat`(월드, 플레이어 아래 층) + `drawThreatWarning`(화면, 전경 위). 크롬 검수: gap 240(경고만) ·
  110(벽 진입) · 60(눈·촉수) · 잡힘 → 카드. 탭이 hidden이면 `__jgh.stepOnce`로 구동 (HANDOFF 참고)
- 계측 `game_over.cause` 추가. 프리셋 `nothreat`·`chase2`. BUILD 19

## 2026-08-30 — BUILD 20 일수 표기 (D-014)

- `daysOf(m) = floor(m·365/1000)`, 렌더러 4곳(HUD·카드·카운트업·최고 칩) 교체. 테스트 48개. 크롬 검수: 700m 지점
  "255일 / 최고 406일(=1115m)", 잡힘 카드까지 확인

## 2026-08-30 — BUILD 21 찬스 잎에서 폭풍 정지 (실기기 피드백)

- 사용자: "왼쪽 벽괴물은 좋은데 슈퍼 모드 회전 중에 벽에 부딪혀 죽는다". 찬스 잎에 걸리면 폭풍이 450px 뒤로 물러나
  `sonic.chance` 동안 멈춤(D-013 추가). 회귀 테스트: 폭풍 코앞에서 찬스 잎 잡고 12초 매달려도 생존, 놓으면 재개. 테스트 49개

## 2026-08-30 — BUILD 22 슈퍼 대시 200m (실기기 피드백)

- 사용자: B21에서 278일(≈762m, B18 최고 1115m의 68%). "찬스를 잘 잡기 힘드니 찬스로 나아가는 일수를 늘리자" →
  `sonic.dashMeters` 150→200 (55일→73일). 벽괴물 속도는 그대로 — 두 노브를 동시에 바꾸지 않는다.
  찬스 잡기 자체(자석·다음 잎으로 넘어가기) 제안은 아직 미결정

## 2026-08-30 — BUILD 23 찬스 랜덤 발동(k번째 잡기) · 게이지 0.06 (D-015)

- core: `sonic.grabsInStage`·`lastGrabIdx`·`sonicChanceK`. 잡는 순간 stage별 서로 다른 앵커 수를 세고 k와 같으면 찬스.
  위치 기반 찬스(`sonicChanceAnchor`)와 앞의 금색 후광 제거 — 매달린 찬스 잎만 금빛. 테스트 49개(슈퍼·벽괴물 테스트를
  `grabAnchor` 헬퍼로 재작성)
- 봇 종단 계측(찬스면 게이지 맞춰 놓는 봇, 90초, 시드 8): k[2,6] 첫 찬스 13~15m·대시 7회·1600~1800m / k[3,10] 첫
  20m·1300~1900m / k[4,14] 첫 35m·6회. 거리는 k와 무관(봇은 게이지 100%) → [3,10] 채택
- B22(대시 200m)는 배포 없이 B23에 합침

## 2026-08-30 — 계절 나무 설계도 (클로드 디자인)

- 사용자 "배경 나무 수정 — 겨울엔 앙상한 가지만, 여름엔 아주 초록 풍성하게". `design/trees/gen.py` → 해부도 + 사계절 보드 5장.
  규칙 하나(가지 뼈대 + 잎 밀도)로 사계절을 만들어 렌더러에 한 함수로 옮길 수 있게 함. 캔버스 링크는 design/README.md.
  렌더러 반영(drawCanopyLayer 교체)은 판정 뒤

## 2026-08-30 — 아이폰 더블탭 돋보기 차단 (실기기 피드백)

- 캔버스 더블탭에 iOS 텍스트 선택 루페가 뜸. index.html `-webkit-touch-callout: none` + pointer.ts touch 이벤트 non-passive
  preventDefault(+contextmenu·dblclick·gesturestart). 플레이북 §9에 기록. 실기기 확인은 다음 배포(B24)에서

## 2026-08-30 — BUILD 24 슈퍼 도전 대기·매뉴얼 카드·게이지 랜덤 (D-016)

- core: `sonic.pending`·`sweetCenter`, `press`가 대기 중 새 누름이면 도전 시작(히트스톱 중 무시), `releaseInput`은 대기 중
  no-op, `update`는 대기 중 정지. `sonicSweetCenter(seed, stage)`. 테스트 51개(도전 대기 2 추가, 봇은 "대기면 떼었다
  다시 누름")
- 렌더: `drawSuperPrompt`(카드/배지) + `drawSuperDemo`(4.6초 루프) + 게이지 틀·마커를 헬퍼로 분리(sweetCenter 반영).
  main: 매뉴얼 노출 횟수·"다시 안 보기" 히트. 크롬 검수: 카드·체크 토글·도전 시작 확인
- `dayLabel` 365일↑ "Y년 D일". B24 = 위 + iOS 더블탭 수정. 배포

## 2026-08-30 — BUILD 25 계절 나무 렌더러 반영 · 태양 후광 여름만

- `src/render/tree.ts`: design/trees 규칙 포팅 — `skeleton(seed, depth)` 캐시(높이 1 정규 좌표, core Rng 결정론),
  `drawTree`(잉크 패스 → 밑동 → 가지 채움(굵기 버킷별 한 패스) → 잎덩어리(아래 dark·가운데 base·위 hi) → 반사광),
  `drawTreeSprite`(계절·잎 밀도·색별 오프스크린 캐시 40장, drawImage). 잎 밀도 봄 0.45·여름 1·가을 0.75·겨울 0,
  경계에선 밀도·색 보간(스프라이트 키 때문에 blend 0.1 단위 양자화)
- 렌더러: `drawCanopyLayer` 제거 → `drawTreeLayer` 먼 나무 4(실루엣, 땅 0.76h) → 숲 띠(forestFar) → 중간 나무 2(기둥, 땅 1.02h).
  색 매핑 hi=forestFar·base=forestMid·dark=forestNear·trunk
- 태양: 테두리 밖 후광은 여름 비중만큼만 (사용자 "봄·가을·겨울의 그 테두리가 AI 그림 같다, 겨울은 더 작아 보여야")
- 크롬 검수: 사계절 스트립 캡처(순간이동 시 앵커 해제·찬스 k 9999로 캡처 스크립트 수정). 정상 상태 렌더 2.4~3.9ms/프레임
  (맥 DPR 2, 눈·낙엽 포함). 직접 stroke 시 5ms → 스프라이트로 완화

## 2026-08-30 — 세션 마무리: 형제 앱 더블탭 배포 · 경로 이동

- 한줄팡(`toss/hanjulpang`, 출시됨) 테스트 배포 `01a0532c…` · 슈팅스타(`toss/shootingstar`, 검수 단계) BUILD 30 테스트 배포
  `01a0532d…` — 둘 다 더블탭 돋보기 패치, URL 문자 전송. **한줄팡 검수 요청은 콘솔 MCP가 세션에 없어 미진행** (HANDOFF 1번)
- 작업공간 정리: junglehook → `toss/junglehook`, games → `toss/hanjulpang`, dot → `toss/shootingstar` (리포명과 동일)
- HANDOFF를 현재 스냅샷으로 재작성 (B9·B11 판정 항목 등 옛 이력 정리)

## 2026-08-31 — BUILD 26 슈퍼 도전 다듬기 · 나무 스프라이트 경계

- 사용자 피드백 3건: ① "떼었다가 누르면" 문구 이상 → 규칙을 "꾹 누르면 도전, 한 바퀴 전 놓으면 대기 복귀, 한 바퀴 뒤 놓으면
  포기"로(D-016 추가). 봇(game.test·sim-threat)도 도전을 끝까지/한 바퀴 뒤 포기로 수정 ② 데모가 안 보임 → 카드 2회 제한 해제
  ③ 나무 윗부분 잘림 → `tree.ts` 스프라이트 크기를 뼈대 경계(가장 높은 마디+최대 잎 반지름)에서 계산. 테스트 53개
- 크롬 검수: 여름 나무 윗부분 온전, 매뉴얼 카드 데모 두 시점(회전·게이지) 캡처. 검수 스크립트 함정 추가: 저장값은 모듈 로드 때
  읽으므로 localStorage를 바꾼 뒤 iframe reload 필요
- 사용자 "구간 안쪽인데 실패": 표시(지난 프레임)와 판정(놓는 순간) 사이 터치 지연만큼 마커가 앞서 있었다 → `sonic.inputGraceSec`
  0.09, `sonicInSweet`가 직전 90ms 세 시점을 본다. 테스트 54개. 빌드 표식 B26 그대로(미배포)
- 사용자 "겨울을 흑백으로 표현할 필요 없다 / 눈이 오니 흐린 날이긴 해야": 겨울 팔레트를 회색조 → 추운 색으로. 푸르스름한 흐린
  하늘(#a6bdd6→#e3ebf3), 푸른 회색 구름, 창백한 해, 눈 언덕(forestFar #eaf1f6), 갈색 가지(trunk #7a5a44), 상록 양치식물(leaf #3f6f5e).
  design/trees·day-seasons 캔버스의 겨울 보드는 옛 회색조 그대로(설계도라 미갱신)
- BUILD 26 배포 `01a05338…` + 문자 전송 (2026-08-31). 세션 마무리 — 다음은 B26 실기기 판정(HANDOFF 0번), 한줄팡 검수 요청(1번)

## 2026-08-31 — BUILD 27 슈퍼 대시 1년 1회 (D-017) · 매뉴얼 카드가 안 뜨던 원인

- 난이도 계측 → 찬스 단계를 250m에서 1000m로 (D-017). `tuning.sonic.chanceStepM` 신설, k [3,10]→[12,50]
- **"슈퍼 예시 애니메이션이 아예 안 나온다"(실기기)** — 카드 자체가 안 뜬 것이고, 원인 후보 셋을 모두 막았다:
  ① `hitSuperPrompt`의 "다시 안 보기" 히트 영역(150×44)이 주 액션 문구 "꾹 누르면 도전!"(cardY+250u) 바로
  아래 6u 지점에서 시작했다 — 도전하려고 화면을 누른 손가락이 체크를 켜면 카드는 **영구히** 사라지고, 카드가
  없으니 체크를 되돌릴 방법도 없다 → 체크박스를 카드 맨 아래(cardY+300u)로 내리고 히트 영역 142×38로 축소
  ② `loadSuperManual`의 저장 불가 환경 기본값이 `hide: !canPersist()`라 localStorage가 막힌 WebView에선
  처음부터 영원히 안 뜬다 — BUILD 26의 "다시 안 보기 전까진 항상"과 정반대라 `hide: false`로
  ③ 폰에 이미 저장된 `hide: true`를 한 번 버리려고 키를 `jgh.v1.super-manual2`로 올림
- 테스트 54개 통과. BUILD 27 배포

## 2026-08-31 — BUILD 27 (2) 찬스 발동을 그 해 랜덤 지점으로 (D-018)

- 사용자 "매년 시작이 아니라 1년 중 랜덤". k번째 잡기 → 구간 비율 랜덤 지점 뒤 첫 잡기(`sonicChanceAtPx`).
  `sonic.chanceGrabMin/Max` 제거, `chanceFrom/chanceTo/firstChanceTo` 신설. `SonicState.grabsInStage`·`lastGrabIdx` 제거
- **LCG 시드 파생 버그**: `seed ^ 상수`로는 첫 출력이 거의 같아 찬스 지점이 모든 시드에서 148m, 게이지 중심도
  사실상 고정이었다 → `stageSeed()`로 교체. 1년차 185~280m, 이후 121~834m로 분산 확인
- 테스트: `chanceIdx`/`grabToChance` 헬퍼로 재작성, 54개 통과. 봇 재계측 세션 102~118초·대시 1.9~2.0회

## 2026-08-31 — 세션 마무리: BUILD 27 배포

- 테스트 배포 `01a0579e…` (20:40) + URL 문자 전송. 실기기 판정은 다음 세션(HANDOFF 0번)
- 이번 세션 흐름: 사용자 "너무 쉬운 것 아닌가" → 봇으로 확인(슈퍼 켠 계측이 없어 못 보고 있었다) → 대시 축소안은
  사용자가 기각("130m는 매리트 없다") → 1년 1회(D-017) → "매년 시작 말고 1년 중 랜덤"(D-018) → 랜덤 자체가
  고정이던 LCG 버그 발견·수정. 곁가지로 "예시 애니메이션이 안 나온다"의 원인(카드 영구 OFF)을 찾아 셋 다 막음
- 핵심지표(콘솔 전환 지표) 논의는 슈팅스타 건이라 여기서 중단 — 정글훅 `analytics.ts`는 이식된 뼈대 그대로

## 2026-08-31 — BUILD 28 대시 화염을 셋으로

- 사용자 "슈퍼 대시 뒤 불꽃이 미비하다 — 세모 꼬깔 하나뿐. 작은 화면 둘 + 큰 화면 하나로 나눠 움직이게".
  기존 `flame()`은 **같은 꼭짓점**에서 크기만 다른 삼각형 3겹이라 색을 나눠도 한 덩이로 읽혔다
- `drawPlume(ox, oy, len, half, ang, ph, s, now)` 신설: 노즐에서 뒤로 뻗는 카툰 불꽃 하나. 2차 베지어 두 줄로
  배가 부른 실루엣, 안쪽 두 겹(주황·노랑)은 외곽선 없이 색만 겹쳐 심지가 뜨겁게. 큰 화염(len 120·half 19, 정면)
  뒤에 작은 둘(74/66, ±0.62·0.68rad, y ±11)을 **위에** 그린다 — 순서를 반대로 하면 작은 화염이 큰 화염
  아가리에 삼켜진다
- 애니메이션: 길이 `pulse`와 꼬리 흔들림 `wob`의 **주기 자체를** 화염마다 다르게(38/44/51ms, 52/62/73ms).
  위상만 다르면 셋이 같은 박자로 커졌다 작아져 다시 한 덩이로 보인다
- 크롬 검수(`/tools/frame.html` + `__jgh`로 `sonic.dashing` 강제): 프레임마다 세 갈래 길이가 따로 논다.
  테스트 54개 통과. 렌더 전용 변경이지만 폰 피드백용으로 BUILD 28
- BUILD 28 배포 `01a057f4…` + 문자 전송 (2026-08-31 22:14). B27 배포분(`01a0579e…`)은 화염 전 버전이라
  난이도·화염을 이 빌드 하나로 함께 판정하면 된다

## 2026-08-31 — BUILD 29 최고기록 칩을 거리 칩 아래로

- 실기기 스크린샷: 최고기록 칩이 태양과 겹쳤다. 우연이 아니라 구조적 충돌 — 태양은 화면 고정(`w-75u`,
  `topInset+96u`)이고 여름엔 R이 56u까지 커져 오른쪽 정렬 칩(`w-14u`)과 매 여름 겹친다
- 후보 셋(왼쪽 스택 / 상단 가운데 / 태양 이동)을 놓고 사용자가 **왼쪽 스택** 선택. `drawHud`의 best 칩을
  `bx = 14u`, `by = top + dh + 6u`로. 같은 단위(일수) 두 숫자가 세로로 붙어 비교가 쉽고, 가운데는 로프·앵커가
  지나가는 길이라 오히려 겹칠 확률이 높다
- 크롬 검수: 여름 구간(375m) + best 1366으로 캡처해 태양 자리가 완전히 비는 것 확인. 테스트 54개 통과

## 2026-08-31 — BUILD 30 iOS 브라우저에서 캔버스가 주소창 뒤로 밀리던 것

- 로컬 개발서버를 폰(아이폰 크롬)으로 열었더니 HUD 두 칩이 안 보이고 화면 아래에 하늘색 띠가 남았다
- 원인: `#game`은 `position: fixed; top: 0`인데 iOS(사파리·크롬 모두 WebKit)에서 fixed는 **레이아웃 뷰포트**
  기준이다. 주소창이 떠 있으면 시각 뷰포트가 그만큼 내려가 있어 캔버스 윗부분이 주소창 뒤로 들어가고,
  높이는 `visualViewport.height`라 아래쪽에 body 배경(`#bfe8f5`)이 드러난다
- 수정: `resize()`에서 `canvas.style.top/left`를 `visualViewport.offsetTop/offsetLeft`로. 주소창이 접히고
  펴질 때는 resize가 아니라 **scroll**로 오므로 `visualViewport`의 scroll도 듣는다.
  토스 WebView는 주소창이 없어 둘 다 0 — 배포본 동작에는 영향 없다
- 같은 세션 실기기 확인: HUD 왼쪽 스택(B29)이 의도대로 보이고, 매뉴얼 카드(B27 수정)도 정상 복구됐다

## 2026-08-31 — BUILD 31 HUD 정렬 · 화염을 다시 하나로 · 푸른 스파크 기각

- **HUD 정렬** (사용자 "점수 영역과 태양 영역의 줄이 어긋나 보인다"): 태양은 계절마다 R이 28~56u로 변해
  가장자리가 고정 기준이 못 되고 중심(`topInset+96u`, 여름 광선이 화면 위로 안 잘리는 값)만 고정이다.
  HUD 위 여백을 14u→30u로 올려 **최고기록 칩의 중심을 태양 중심에 맞췄다**(30+44+6+16=96u).
  크롬 검수로 가을·여름 모두 확인 — 여름 광선도 칩과 안 닿는다
- **화염**: 사용자 스케치대로 세 갈래를 거의 나란히 눕히고 몸통 반지름(22u) 안에 넣었으나, 사용자 "세 갈래는
  좀 이상하다" → **굵은 화염 하나 + 불똥 5개**로. 캐릭터가 44px 남짓이라 갈래 사이 틈이 2px뿐이고 속도선·번개·
  잔상과 겹쳐 빗살 노이즈로 읽힌다. 이 크기에서 불을 파는 건 갈래 수가 아니라 움직임 — B28의 성과인
  실루엣(배 부른 곡선)과 길이·꼬리 애니메이션은 유지하고 개수만 하나로
- **푸른 스파크 기각** (사용자 "스파크가 살짝 푸른빛이 돌아도 되려나"): 아크가 6/3.5/1.8px로 얇아 안쪽만
  식히면 파랑이 아니라 흐린 분홍으로 읽힌다. 확실히 파랗게 하려면 아크 전체를 식혀야 하는데 그러면 D-012의
  근거(시스템 노랑 → 발동색 인과, "주인공이 불붙은 것")가 깨진다 → 붉은 광채 + 흰 심 유지

## 2026-08-31 — 화염을 손그림 선질로 · 전기 아크 다시 (BUILD 31 계속)

- 사용자 피드백 연쇄: 세 갈래 → "좀 이상하다" → 하나 + 불똥 → "직선 말고 비뚤빼뚤" → 톱니 → **"너무 컴퓨터
  그림 같다 / 길게 뻗는 게 아니다"**. 문제는 모양이 아니라 **선질**이었다 — 계산식으로 만든 좌우 대칭 곡선은
  아무리 흔들어도 기계로 보인다
- `drawPlume` 재작성: ① 위·아래 가장자리가 **서로 다른 난수**를 써 대칭이 생기지 않는다 ② 기본 그림은 고정
  난수(`base`)로 두고 90ms마다 갈리는 `wob`으로 **작은 흔들림만** 얹는다(= 전통 2D 애니메이션의 boil).
  매 프레임 통째로 새로 뽑으면 "찢어진 조각"이 튀고, 안 흔들면 죽은 그림이 된다 ③ 외곽선을 조금 어긋나게
  **두 번 긋는다** — 한 번이면 자로 그은 듯 반듯하다. 길이 128u→92u, 반폭 17u→21u (짧고 넓은 덩이)
- `drawLightning` 재작성 (사용자 "거미 다리 같다"): 6px 굵기·마디 3~4개가 사방으로 뻗던 것을 **얇은 심
  (1.4px) + 옅은 광채, 잔마디 7~10개, 짧은 가지**로. 시작 방향을 뒤(π) 쪽으로 몰아 앞으로 뻗지 않게 했다

## 2026-08-31 — HUD를 태양 중심에 맞추고 함께 위로 · 데모 문구에서 "앵커" 제거 (BUILD 31 계속)

- 사용자가 정렬 후보를 디자인 캔버스에서 보고 결정: **HUD 두 줄 블록의 세로 중앙 = 태양 중심**.
  블록은 44+6+32=82u라 중심이 `top+41u` → `top = 태양중심 - 41u`. 둘 중 하나만 옮기면 관계가 깨진다
- 이어서 "좀 더 위로": 태양 중심 96u→**76u**, HUD `top` 55u→**35u**. 한계는 토스 헤더가 아니라
  **여름 광선**이다 — 광선 끝이 중심에서 92u라 76u면 위가 조금 잘리지만 "모서리에 걸친 태양"으로 읽힌다.
  좌표가 전부 `topInset` 기준이라(`cancelHostTopInset`) 네이티브 헤더와는 겹치지 않는다
- 시작 화면 데모 문구 "다시 누르면 다음 **앵커**에!" → "다음 **잎으로**!" (사용자: 일반인이 못 알아듣는다).
  화면에 실제로 보이는 것도 덩굴 끝의 잎이다. 코드 안 용어(anchor)는 그대로 둔다
- 디자인 캔버스 게시: HUD 정렬 4안 + 대시 화염 실루엣 5안 (design/canvas, 링크는 세션 기록)

## 2026-08-31 — 화염을 원 사슬 윤곽으로 다시 (BUILD 31 계속)

- 사용자 "화염은 다 별로다 / 🔥 이모지 정도만 되어도 좋겠다". **앞선 시도가 전부 실패한 원인은 모양이 아니라
  방법**이었다 — 사인파 물방울·톱니·시드 난수 모두 곡선을 계산식으로 만들어서, 아무리 흔들어도 기계로 읽힌다
- 방법 교체: **크기가 줄어드는 원을 사슬처럼 늘어놓고 그 바깥 윤곽만 딴다.** 만화 불·구름·연기의 정석이다.
  윤곽이 원의 호와 공통 외접선으로만 이루어져 통통한 불꽃 실루엣이 저절로 나온다
  - 이음매 각: `alpha = acos((r_i - r_{i+1}) / d)` — 반지름 차이를 반영해야 접선이 원에서 안 뜬다
  - 호는 잘게 샘플링해 선분으로 잇는다 (방향 부호 실수로 윤곽이 뒤집히는 사고를 막는다. 첫 구현이 이걸로 깨졌다)
  - **반지름을 단조 감소시키면 매끈한 원뿔**이 된다 → 중간중간 되키워서(0.74 → 0.84) 이음매를 꺾어
    불꽃 특유의 혹을 만든다. 사슬은 끝으로 갈수록 위로 휘어 혀끝이 말린다
  - 마디마다 다른 주기로 반지름·위치가 흔들려 프레임마다 혹의 위치가 바뀐다. 길이 86u·반폭 21u

## 2026-08-31 — 화염: 면을 칠하지 말고 **획을 긋는다** · 스파크 제거 (BUILD 31 계속)

- 화염 시도 다섯 번째. 사용자가 크레용 로켓 그림 두 장을 보여주고 "이런 느낌" → 그림을 보고 알아챈 것:
  **저 불은 면을 칠한 게 아니라 굵은 획을 몇 번 그은 것**이다. 앞선 네 번(물방울·톱니·손그림 boil·원 사슬)은
  전부 닫힌 면을 칠하는 방식이었고, 면은 아무리 흔들어도 도형으로 읽힌다
- `drawFlameFan`: 노즐에서 부채꼴로 **둥근 끝(lineCap round)의 굵은 획**을 3·3·2개 긋는다(빨강 25u ·
  주황 18u · 노랑 13u). 획 끝이 그대로 둥근 혹이 되고, 길이가 제각각이라 끝이 삐죽삐죽해진다. 외곽선 없음
- 사용자 "가운데 화염은 자리를 잡고 있어야 한다, 지금은 오징어 다리 같다" → 가운데 획일수록 진폭을 0에
  가깝게(`mid` 가중치), 바깥 획만 일렁이게. 전부 같이 흔들면 다리처럼 보인다
- 사용자 "스파크가 해파리 같다, 없애자" → `drawLightning` 호출과 메서드 삭제. 대시 연출은 이제
  속도선 + 잔상 + 화염 + 불똥 몇 개다

## 2026-08-31 — 잎이 가을 내내 지고 겨울 초입에 마저 떨어진다 (BUILD 31 계속)

- 사용자 "겨울나무만 앙상할 게 아니라 가을부터 차츰 줄어들게 / 겨울 오면 훅훅 줄어들게"
- `LEAF_DENSITY` 상수 맵 → `leafDensityAt(season, progress)`: 봄 0.30→0.68 · 여름 1 · **가을 0.85→0.28** ·
  **겨울 0.28→0(구간의 40% 안에)**. 경계 보간은 이전 계절의 **끝** 밀도를 쓴다(가을 끝 0.28 = 겨울 시작 0.28이라 이어진다)
- 그런데 이것만으로는 사용자가 "겨울 되자마자 싹 없어졌다"고 했다. 원인은 `tree.ts`가 **밀도로 잎덩어리의
  반지름만 줄이고 개수는 그대로**였던 것 — 가을 내내 "조금 작아진 잎"이다가 0.2 문턱 아래에서 덩어리가
  통째로 사라졌다. → 마디마다 시드로 고정된 문턱을 두고 밀도가 그 아래면 그 잎이 진다(개수가 준다).
  안쪽 굵은 가지의 문턱을 낮춰 바깥 잔가지 잎부터 떨어진다. 남는 덩어리는 크게(scale 0.62+0.38·leaf)
- 스프라이트 재생성이 잦아지지 않게 밀도 양자화를 0.02→0.05로 (가을 한 구간에 12번 남짓)
- 크롬 검수: 520→620→720→770→820→880m 스트립에서 잎이 단계적으로 줄고 겨울 초입에 마저 떨어지는 것 확인

## 2026-08-31 — 계절 진행 검수 도구 · 가을 낙엽 곡선 더 내림

- 사용자 "잎이 가을부터 차츰 지는 건 확인이 안 됐다" — 당연했다. 가을·겨울 나무를 보려면 500m 넘게
  살아남아야 해서 **플레이로는 확인이 사실상 불가능**했다
- `tools/seasons.html` 신설: 게임을 숨겨 띄우고 개발 훅으로 구간마다 순간이동시켜 그때 화면을 오려
  이어 붙인다. 버튼으로 1년 전체 / 가을만 / 겨울만 / 2년차를 뽑는다. 폰에서도 열린다
  (`npm run dev -- --host` → `http://<맥 LAN>:5173/tools/seasons.html`)
- 그 스트립으로 보니 가을 97% 지점(743m)에서도 아직 무성했다 → 곡선을 더 내렸다:
  가을 0.85→0.28을 **0.90→0.15**로, 겨울은 구간의 40%가 아니라 **30% 안에** 0으로.
  이제 686m에서 가지가 드러나기 시작하고 743m는 성글며 800m 겨울이 자연스럽게 이어진다

## 2026-08-31 — 세션 마무리: BUILD 31 배포

- 테스트 배포 `01a0584b…` (23:49) + URL 문자 전송. B29~31이 한 번에 올라갔다
- 이번 세션 흐름: "너무 쉬운가" → 봇 계측(슈퍼 켠 표가 없어 못 보고 있었다) → 슈퍼 1년 1회(D-017) →
  발동 지점 랜덤(D-018) + LCG 시드 버그 → 매뉴얼 카드 영구 OFF 버그 → HUD 정렬(디자인 캔버스로 4안 비교) →
  화염 다섯 번 실패 후 "획으로 긋기"로 안착 → 잎이 가을부터 지게 + 검수 도구(`tools/seasons.html`)
- 저장소: origin/main에 41+2 커밋 푸시(BUILD 17 → 31). 디자인 캔버스 게시본은 커밋 제외로 정리
- 남은 것: B31 실기기 판정 → 효과음(HANDOFF 1번) → Phase 1 잔여(점수 제출·뒤로가기 모달)


## 2026-09-03 — 이름 변경: 정글훅 → 날아날아 (BUILD 32)

- 사용자 "이름을 좀 쉬운 걸로 — 발음하기 쉬운 두 글자 반복". 후보를 동작별로 30개 남짓 뽑아 동명 검색
  (D-002 기준). 하루하루(앵커 하나 ≈ 하루 이틀이라 뜻은 최고)는 동명 앱 다수로 탈락, **날아날아** 확정 (D-019)
- 슬러그까지 교체: appName `naranara` · `intoss://naranara` · 저장 `nara.v1` · 계측 `nara_` · 개발 훅 `__nara`
  (`tools/seasons.html`도 함께). 옛 `jgh.v1` 저장값은 버림. 리포명은 junglehook 유지
- 표시 이름: index.html · 타이틀(renderer) · 문자 문구(imessage.mjs) · 디자인 보드 원본 · 문서 머리
- 콘솔은 `naranara`로 새로 등록해야 한다 — 로컬 세션에서. 그때까지 B32는 배포 안 됨
- 검증: npm test / npm run build


## 2026-09-03 — 리포명 변경: junglehook → naranara

- 사용자가 GitHub 리포를 `jsypsy/naranara`로 개명 (D-019에 추가 기록). 위 "리포명은 junglehook 유지"는 이로써 뒤집힘
- 문서의 현재 상태 서술만 갱신: HANDOFF 리포·경로 줄, D-019, design/README 게시본 파일명. 과거 기록(D-002,
  `tools/latest-deployment.json`의 옛 슬러그 B31 배포)은 이력이라 그대로 둠
- 코드는 손댈 곳 없음 — package.json `name`·appName·딥링크·접두어는 B32에서 이미 `naranara`
