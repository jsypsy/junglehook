# 결정 기록

역순 아님 — 번호순. 각 결정은 번복 시 여기에 취소선 대신 후속 D-번호로 잇는다.

## D-001. 장르: 원버튼 그래플 스윙, 설계 1순위: 리워드 광고 수익 (2026-08-30)

세 번째 앱의 축을 "게임(원버튼 하이스코어) + 수익(리워드 광고)"으로 확정.
후보 비교(막대 다리·탑 쌓기·궤도 전환·그래플 스윙·줄넘기·외줄 균형·동굴 활공·
회전 바늘) 끝에 **그래플 스윙** 선택. 근거:

- 홀드 → 떼는 타이밍이 전적으로 플레이어 판단 → 죽음이 100% 납득 → 이어하기 전환율
- Stick Hero처럼 원조 한 작품이 소유한 메커니즘이 아니라 장르가 퍼블릭한 영역
- 관성 스윙은 점수와 별개로 손맛이 존재 → 리텐션 보험
- 그릴 것이 앵커·로프·캐릭터·배경뿐 → 스프라이트 부담 최소 (슈팅스타의
  플레이스홀더 아트 반복을 피한다)

**수익에서 역산한 설계 원칙 5** — 규칙을 바꿀 때 이 5개를 깨는지 먼저 본다:
1. 점수는 누적형(거리) — 한 번에 잃어야 아깝다
2. 즉사 — 목숨 분할은 아까움을 희석한다
3. 죽음이 내 실수로 납득돼야 한다 — 억울하면 광고가 아니라 이탈
4. 세션 30~60초 — 너무 짧으면 잃을 게 없고, 길면 세션 수가 준다
5. 이어하기 판당 1~2회 제한 — 점수 인플레·어뷰징(abuseLevel) 방지

## D-002. 이름: 정글훅 (junglehook) (2026-08-30)

검색 검증 결과로 확정. 리포·슬러그·appName·storage 접두어(`jgh.v1`)·
이벤트 접두어(`jgh_`) 모두 이 이름 기준.

- 탈락: **스윙팡** — (주)스윙팡(스크린골프 회사, Google Play 앱 보유) 실존.
  슈팅스타 개명 사태(제명 중복 → D-010/D-011) 재현 위험
- 탈락: **몽키 계열** — 몽키훅이라는 **동장르(원터치 훅&스윙) 앱이 이미 존재**.
  원숭이 캐릭터 테마도 함께 피한다
- 탈락: 훅팡·로프팡 — 사용자가 팡 패밀리 브랜딩을 원치 않음
- ⚠️ GRAC 제명은 신청 직전 GRAC 등급분류 검색으로 **한 번 더** 동명 확인 후 확정

## D-003. 스캐폴딩: 슈팅스타에서 이식, 모노레포 안 묶는다 (2026-08-30)

한줄팡→슈팅스타 때의 결정(모노레포는 이 규모에서 번들 최적화 복잡도만 늘린다)을
승계. 이식 목록은 CLAUDE.md 참조. 복사본이므로 원본 수정이 자동으로 안 넘어온다.
`sound.ts`는 기동·복구 뼈대(한줄팡 D-009~D-012 산물)만 유효하고 효과음 메서드는
슈팅스타 것 — 게임 구현 때 교체한다.

## D-004. 확률·아이템 요소는 초기 버전에서 제외 (2026-08-30)

순수 실력 게임으로 시작한다. 근거: GRAC 등급분류가 가장 단순해지고, 등급 후
밸런싱 변경이 내용수정신고에 걸릴 면적이 최소화된다 (슈팅스타가 지금 이 제약에
묶여 있다). 단 **내용설명서에는 "추후 추가될 수 있는 것"(이어하기 광고, 소모성
아이템 등)을 미리 기재**해 재신고를 아낀다 (플레이북 §4 교훈).
나중에 아이템을 실제로 넣을 때는 내용수정신고 여부를 먼저 확인한다.

## D-005. 조작 규약과 물리 구성 (2026-08-30, 프로토타입 계측으로 확정)

- **선입력 버퍼링**: 홀드 중 로프가 없으면 매 프레임 타깃을 잡으려 시도 —
  reach 밖에서 미리 눌러도 닿는 순간 잡힌다 (억울한 죽음 방지, 원칙 3)
- **타깃 하이라이트**: "지금 누르면 잡힐 앵커"를 매 프레임 노란 링으로 표시
- **로프 감기(reel-in) 110px/s**: 순수 진자만으로는 고도 회복이 불가능해
  매 사이클 가라앉다 죽는다는 것을 봇 계측으로 확인 → 홀드 중 로프가 감기며
  각운동량 보존으로 속도·고도를 회복한다. 스킬 축(오래 감기 vs 빨리 전진)도 겸함
- **공기 저항 0.12/s**: reel-in이 계속 넣는 에너지의 배출구. 없으면 속도가
  무한히 붙는다(계측: 700px/s 돌파 후 계속 상승) → 드래그로 속도 상한 형성
- **reach 270→340**: 낮은 비행에서 앵커선(y≈175)까지 수직 거리가 300을 넘어
  "잡을 로프가 아예 없는" 구간이 생기는 것을 계측으로 확인
- 검증: 고도 적응형 봇이 30초 생존·314m, 속도 350~720px/s에서 안정
  (`src/core/sim-observe.test.ts` — 평소 skip, 튜닝 시 재사용)

## D-006. 물리: 무감쇠 진자 + 스윙 펌프 — "오래 돌수록 세진다" (2026-08-30, 실플레이 피드백)

- **문제**: 첫 실플레이(156m·206m)에서 "로프에 수직으로 매달린 채 좌우 운동을 잃어
  다음 앵커로 갈 방법이 없는" 상태 발생. 계측으로 원인 확인: 구속 투영이 매 스텝
  적분기 오차(-½g²dt², g=2000이라 초당 16,700)를 상쇄 못 하게 끊어 **진자가 수 초
  만에 멈춤**(60°→2°/15s). 탈출로(놓고 0.3초 안에 다시 누르면 다음 앵커가 잡힘)는
  있었지만 매달린 동안 타깃 링이 안 보여 알 수 없었다
- **결정 1 — 에너지 보존 투영**: 스텝 시작 에너지 기준으로 투영 후 속력을
  v0²+2gΔy에 맞춘다. 30초 뒤에도 진폭 유지(60°→58.7°). 90° 위로는 로프가 느슨해졌다
  스냅하며 에너지가 빠져 자연히 수평에서 캡
- **결정 2 — 감기(reel-in) 분리**: 윈치가 외부에서 일을 넣는 연산이라 투영과 분리
  (`reelIn`). 정확한 에너지 보존을 감기에도 적용하면 상승분만큼 속력을 빼앗아 모든
  봇이 5m에서 매달린다(계측). `reelGain` 0 = 속력 유지, 1 = 각운동량 보존. 0으로 시작
- **결정 3 — 스윙 펌프**: 사용자 제안("한 앵커에 매달려 붕붕 돌수록 점프력이 세지게").
  매달린 동안 진행 방향으로 350px/s² 가속, 상한 1100px/s(한 바퀴 회전 최저 속도
  √(5·g·minRope)≈950 위). 홀드 2~3초면 회전, 놓으면 ~1100으로 발사. 상한 없으면
  47,000px/s까지 폭주(계측). 스킬 축: "얼마나 돌리고 언제 놓나"
- **난이도 곡선 rampX 9000→18000**: 180m(≈15초)에 최대 난이도라 세션이 목표
  (30~60초)의 절반. 봇 계측 생존 중앙 21s→33s
- 봇 검증: angle30 봇 20시드 60초 전원 생존·746m — **너무 쉬워졌을 가능성** 있음.
  실플레이 후 gapMax·jitterMax로 조정
- 수익 역산(D-001) 점검: 죽음은 여전히 "놓는 타이밍 = 내 탓". 세션은 길어지는 쪽
- **수정(같은 날, BUILD 4)**: 실플레이 피드백 "360° 회전이 원작 스펙인가 / 너무 빨라 앵커가
  안 보인다". 장르 참조작(Stickman Hook 류)은 진자 스윙이 수평 근처까지이고 회전은 없다.
  → swingPump 350→150, swingMaxSpeed 1100→650(90° 진폭 속도 √(2g·minRope) 아래),
  minRope 90→130. 무한 홀드 최대각 83°·회전 0·각속도 2.46→0.61회전/s. 봇 거리는 유지
- **BUILD 5 "충전기"(같은 날)**: 사용자 판단 "360°는 물리 자유도의 매력이 있는데 속도 때문에
  정신없다" → 회전을 **의도적으로 충전하는 필살기**로 재배치. (1) 홀드 중 로프를 강체(막대)로
  (`rigidRope`) — 느슨한 로프로는 90° 위 스냅 손실 때문에 pump≥1000이어야만 회전이 나
  "기본 상태 아니면 불가능"의 양극단만 있었다. 강체면 pump 200으로 4초 홀드에 첫 회전.
  (2) 투영 배율 [0.5, 2] 클램프 — 정지점에서 잔여 속도를 부풀려 96°에 붙어 기어오르는
  인공물 제거. (3) cap 1050(강체 회전 문턱 √(4gL)≈1020 바로 위)·airDrag 0.2(발사 직후 감속).
  (4) 카메라 속도 줌아웃 600→1200px/s에서 1.0→0.78. (5) `?p=b4` 프리셋으로 B4와 A/B.
  회전 중 각속도 1.8회전/s는 L=130의 물리적 하한(√(4g/L)/2π≈1.6) — 더 느긋하려면 minRope↑

## D-007. 데드라인은 UI가 아니라 지형으로 — 강물 + 사거리 밖 앵커 흐림 (2026-08-30)

- 문제: 사망선(killY 830)이 화면(749) 밖이라 플레이어가 본 적 없는 선에 죽고, 로프 사거리
  (340) 밖 앵커는 "보이는데 왜 안 잡히지"가 된다. 사용자: "데드라인을 알아야 하는데 디자인이
  지저분해질까 걱정"
- 결정: HUD 요소를 추가하지 않는다. (1) **어두운 강물**을 killY 수면으로 그리고 killY를 760으로
  올려 기본 줌에서 화면 아래 끝에 걸리게 — "물에 빠져 죽었다"는 설명 없이 읽힌다. (2) 지금 로프가
  닿는 앵커만 밝게, 사거리 밖은 흐리게 — 기존 타깃 링과 같은 문법. 사거리 원·경고 텍스트·
  깜빡임은 넣지 않는다(죽음이 "내 탓"이어야 한다, D-001)
- reach 340은 유지. 실플레이에서 여전히 억울하면 그때 reach↑
- **수정(같은 날, BUILD 7)**: 강물 띠는 카메라 줌아웃 때 위아래로 움직여 "디자인 버그"로 보였다
  (사용자) → **제거**. killY 770(화면 아래로 공이 완전히 사라지는 지점). 데드라인은 대신
  **사망 연출**이 보여준다: 추락 지점 충격파·주황 파편(26개)·0.35초 흔들림·따뜻한 색 플래시 →
  "추락!" 팝 → 점수 카운트업 → 신기록 배지 → 재시작 힌트(재시작 잠금 1.0s). 피·붉은색·신체
  표현 없음 — GRAC 전체이용가 기준 유지. reach 340→450, targetBehindLimit 60→120으로
  "하단 30%에서 로프를 못 쏜다" 완화. 사거리 밖 앵커 흐림은 유지하되 대비 완화(α 0.55).
  시작 화면에 한줄팡식 3.2초 루프 데모(누르면 매달림 → 떼면 비행) 추가

## D-008. 비주얼 방향: 밝은 카툰 정글 (2026-08-30, 클로드 디자인 캔버스로 확정)

- 사용자가 "밝은 카툰 정글"을 택했다(어두운 밤 정글 대안 대비). 문법: 굵은 외곽선(2px
  #1f3a2a)·평면 색·둥근 형태·흰 카드+오프셋 그림자·태양·구름·3겹 숲. 플레이어는 얼굴(눈·미소)
  있는 주황 공, 앵커는 덩굴 끝 나뭇가지 매듭, 타깃은 노란 링. 웹폰트 없이 system-ui 굵게(번들 0)
- 설계도는 `design/`(캔버스 사본). 구현은 `renderer.ts`(Canvas 2D)에 옮겨 그린다 — 이미지 에셋
  없이 도형·그라데이션으로 재현(런타임 의존성 금지 원칙)
- 게임오버 문구: "추락!"→"놓쳤다!"→**"끝"**(사용자 결정: 짧게). 결과 카드에 쓴다
- 앵커 표현: 구름은 너무 크고 앵커 개수(175~300px 간격)와 1:1로 매핑하기 어렵다 → **구름은
  배경 패럴랙스, 앵커는 덩굴 매듭(잎 뭉치로 키우기)** 으로 확정

## D-009. 이어하기 광고 루프 — 결과 카드에서 바로, 판당 2회 (2026-08-30)

- 사용자: "탭해서 다시가 있으면 광고 보고 이어하기도 있어야지" → 결과 카드에 **주 버튼 "광고 보고
  이어하기 n/2"** + 보조 "다시 하기". 별도 모달 없이 카드 하나로 (탭 수 최소, D-001 1순위)
- 재출발: 마지막으로 지나친 앵커 앞(+75, +130)에서 정상 릴리스와 같은 궤적(520, −300)으로 자유
  비행 시작. 로프를 쥔 채 살리면 버튼 탭의 pointerup이 곧바로 놓아 궤적이 운에 좌우돼서 배제
- 광고: `platform.showRewardedAd('continue')` — 토스는 사전 로딩·재로딩(ait.ts), 브라우저 목은 즉시
  보상. 광고 중 버튼 잠금·"불러오는 중" 표시. 미보상·실패면 카드에 남는다
- 탭 아무 데나 재시작은 제거 — 이어하기 제안을 실수로 건너뛰지 않게. 버튼만 반응(사망 1초 잠금 후)
- 계측 연결: `jgh_game_start`·`deep_play`·`game_over(continued)`·`ad_reward(continue, rewarded)`
- 미결: 광고 중 오디오 mute(sound.ts 연결은 효과음 교체 때), 이어하기 모달 설계도(Continue.dc.html)는
  카드 통합으로 대체

## D-010. 배경 진행 — 하루·계절·날씨 (2026-08-30, 캔버스로 확정, 구현은 BUILD 10)

- 150m마다 아침→낮→저녁→밤→새벽, 750m마다 봄→여름→가을→겨울. 시작은 늘 봄 아침. 경계 100m
  보간(oklch). 500m는 사람 기록(200~400m)상 대부분 못 봐서 기각
- 날씨는 **판 시드로 랜덤**: 150m 단계마다 약 1/4 확률로 비(봄~가을), 첫 단계는 항상 맑음, 겨울은
  항상 눈이고 갈수록 거세짐. 비·눈이면 먹구름이 덮고 광원이 사라진다. 연출 랜덤은 GRAC 확률형
  아이템과 무관(D-004는 보상·성능에 영향 주는 확률만 막는다)
- 앵커 = **덩굴 끝 고리**(갈색 매듭은 "생뚱맞다"). 밤·새벽은 덩굴·앵커에 halo. 겨울 설선은 제거
- 캔버스: https://claude.ai/code/artifact/9f09bbe1-342b-4265-9d59-dc2173636dd6
- **구현 결정(BUILD 10, 같은 날)**: 사용자 "계절은 250m마다, 시간대·날씨는 굳이 필요한지" → **사계절만
  250m 주기**로 구현. 시간대는 250m 주기와 겹치면 색이 너무 자주 바뀌어 보류, 비는 가독성 비용 대비
  이득이 적어 보류(같은 팔레트 보간 구조 위에 나중에 얹을 수 있음). 겨울 눈송이만 유지(진행도에 따라
  개수·속도 증가). 경계 40m 동안 oklab 보간(`render/color.ts`), 계절 진입 글자 안내는 넣었다가 뺐다(사용자: "없이도 충분").
  앵커는 덩굴 고리로 교체(게임·시작 데모 모두)

## D-011. 배경은 "우거진 캐노피" — 언덕 띠 대신 정글 (2026-08-30)

- 사용자: "배경이 산만 있는데 정글훅 이름에 맞춰 바꿔야 하나". 캔버스로 2안(A 우거진 캐노피 / B 강 위의
  정글, 각 봄·여름)을 그려 A 선택. 캔버스: https://claude.ai/code/artifact/f8acb005-732a-42ae-98fa-b8627c656509
- 구현: 먼 나무들(둥근 잎덩어리, 패럴랙스 0.3, 기둥 없음, 몸통을 땅 뒤까지) → 기둥 있는 중간 나무
  (0.5) → 땅 물결(0.75) → 안개 띠 → 앞쪽 양치식물(1.15, 줄기 양옆 잎 쌍). 전부 계절 팔레트(기둥색
  추가). 나무는 띄엄띄엄 — 붙이면 물결 울타리로 읽힌다(첫 시도에서 확인)
- 같은 날 함께: 앵커 = **나뭇잎**(고리는 취소), 봄/여름 팔레트 대비 강화 + 봄 벚꽃잎, 태양이 계절
  캐릭터(봄 미소·여름 광선+화난 얼굴·가을 나른·겨울 눈 감음)
- 보류: 계절별 게임 규칙 변화(여름 바람 / 가을 떨어지는 잎 / 겨울 언 덩굴) — 사용자 선택 대기

## D-012. 소닉 파워 — 회전 3바퀴로 장착, 놓으면 로켓 대시 250m (2026-08-30)

- 사용자 제안: "360° 회전을 몇 차례 돌면 슈퍼 아이템이 장착돼 타이밍 맞춰 놓으면 250m 스트레이트".
  균형 우려(회전이 3배 효율 → 단조로움, 계절 하나 스킵, 세션 연장)를 제시했으나 **"원래 규칙을 무시하고
  슈퍼맨·로켓처럼 앞으로 쭉"** 으로 재확인 → 그대로 구현, 수치는 tuning `sonic`으로 실플레이 튜닝
- 규칙: 한 앵커에서 3바퀴(누적 회전각 기준, 새 앵커를 잡으면 0) → 장착(노란 오라·스파크). 놓으면
  중력·잡기 무시 직진 2500px/s, 순항 고도 230으로 떠오르며 250m. 끝나면 (650, −260)으로 일반 비행 →
  곧바로 다음 잎을 잡아야 한다(내 탓 원칙 유지). 판당 횟수 제한 없음
- 연출: 충전 링(바퀴당 1개), 장착 오라·회전 스파크, 대시 중 잔상·로켓 불꽃·화면 속도선, 줌아웃(속도
  연동). 결과 카드에 "소닉 ×N". 글자 안내 없음
- 규정: 확률·구매 없는 실력 부스트 — GRAC 확률형 아이템과 무관. 내용설명서에 "회전으로 충전하는 부스트"
- 계측: `game_over.sonic` (대시 횟수)
- **타이밍 게이지(같은 날 추가)**: 사용자 "그냥 놓는다고 다 날아가면 안 되고, 게이지 안의 선에 딱 맞았을 때만".
  장착되면 상단에 게이지가 뜨고 마커가 0.7Hz로 왕복, 가운데 노란 구간(±8%) 안에서 놓아야 대시. 밖에서
  놓으면 일반 릴리스 + 충전 소멸. 마커가 구간에 들어오면 노란색으로 바뀌고 위에 점이 켜진다(글자 없음)
- **플래시식 모션 블러**: 대시 중 지금까지 그린 장면을 왼쪽으로 18/40/70px 밀어 겹쳐(self drawImage)
  주변이 뭉개지고 플레이어만 선명하다. 대시 중에만 캔버스 3회 추가 합성
- **찬스 앵커(BUILD 15)**: 사용자 "너무 쉽다 → 계절마다 한 번, 랜덤 앵커에서만". 계절 단계(250m)마다
  시드+단계로 정한 위치(구간의 20~80%)에 가장 가까운 잎 하나가 **금색 후광**의 찬스 잎. 거기에 로프가
  걸리면 **0.45초 멈춤 + 파열 링·방사선·플래시**로 알린다. 충전(3바퀴)·게이지는 찬스 잎에서만. 놓는 순간
  (성공이든 실패든) 그 계절의 찬스는 소모. 랜덤은 연출·기회 배치라 GRAC 확률형 아이템과 무관(보상 확률 아님)
- **슈퍼 모드 외형**: 대시 중 주인공 1.5배·선글라스, **토르식 번개 아크**(45ms마다 다른 패턴) + 전기 오라.
  처음엔 파란색이었으나 사용자 질문("노랑·주황·빨강이 나으려나")에 **따뜻한 계열로 확정**: 백열 몸(#fff1b0,
  어느 계절 배경에도 없는 색이라 대비 유지), 불꽃 빨강→주황→노랑 3겹, 번개 흰 심 + 주황·빨강 광채, 잔상 빨강.
  근거: 시스템 색이 노랑(찬스 잎·충전 링·게이지)이라 발동 색이 이어져야 인과가 읽힌다; 주인공이 주황이라
  "같은 애가 불붙은 것"으로 보인다
- **텍스트 알림(BUILD 17)**: 찬스 잎에 걸린 순간 별 배지 **"SUPER!"** 팝(1.4초). 사용자 요청("애니메이션에 더해
  텍스트로도, 카툰 컨셉으로"), 문구는 "SONIC!"보다 직관적인 "SUPER!"로. 결과 카드 표기도 "슈퍼 ×N". 코드 내부
  이름(sonic*)은 유지


## D-013. 뒤쫓는 폭풍 — 시간 압박으로 "안전 전략"을 없앤다 (2026-08-30, BUILD 19)

- 배경: 사용자 "너무 쉽다". 봇 계측(PROGRESS 2026-08-30)에서 **간격·흔들림·사거리 노브는 안전 전략을 못
  죽인다**는 것이 확인됐다 — "높게 놓고(θ≥35°) 불확실하면 매달리는" 봇은 어떤 조합에서도 120초 0/10 사망.
  D-001 원칙 4(세션 30~60초)를 만들려면 **매달려 기다리는 시간에 비용**이 붙어야 한다
- 결정: 뒤에서 **폭풍(가시덩굴 벽)**이 쫓아온다. 플레이어 x가 폭풍 앞머리보다 뒤면 즉사(`cause: 'caught'`).
  후보였던 "잎이 N초 뒤 찢어짐"은 보수 봇이 스스로 놓으므로 단독으론 부족, "계절별 사거리 축소"는 D-007의
  억울함 비용이 커서 제외
- 규칙 (`tuning.threat`): 시작 600px 뒤 출발 · 속도 300→420px/s(1차 램프 rampX) + 사계절 한 바퀴마다 +40,
  상한 560 · **고무줄** — 플레이어보다 1000px 이상 뒤처지지 않는다(빨리 가도 여유가 무한히 안 쌓임 → 압박
  유지) · 찬스 멈춤(freeze) 중엔 같이 멈춤 · 대시 중엔 계속 전진 · 이어하기 뒤엔 다시 600px 뒤로
- 계측 근거 (봇, 정점 뒤 누름, 시드 8, 120초): 위협 없음 θ=35° 0/8 사망 → 위협 기본 8/8 잡힘 **평균 45s**,
  θ=45° 43s, θ=30° 85s(6/8), θ=25° 94s(5/8). 고무줄 760~1200·속도 변형은 생존 시간 차이가 거의 없었다 —
  죽음은 3~4초 넘는 스톨(초근접 잎을 잡고 제자리 회전)에서 나므로 고무줄은 "스윙 한 주기 + 여유"인 1000으로
- D-001 원칙 3(내 탓): 잡힘은 "내가 지체한 것"이라 납득 가능 — 단, 다가오는 것을 **미리 볼 수 있어야** 한다.
  벽은 화면 왼쪽 126px 안에서만 보이므로 gap 520px부터 왼쪽 가장자리 어둠 + 쉐브론이 맥동(가까울수록 빠르게),
  촉수가 앞머리보다 70~115px 앞서 먼저 들어온다
- 연출: 잉크색 벽 + 파도치는 앞머리 + 촉수 6개 + 소용돌이 잎 + 노란 눈 세 쌍(깜빡임). 피·붉은색 없음(GRAC).
  잡힘 사망은 파편이 앞으로 튕겨 나간다. 결과 카드 문구는 "끝" 그대로
- A/B: `?p=nothreat`(BUILD 18 규칙) · `?p=chase2`(340→480, 고무줄 800). 계측: `game_over.cause` (fall/caught)
- **찬스 잎 예외 (BUILD 21, 실기기 피드백)**: 사용자 "슈퍼 모드 회전 중에 벽에 부딪혀 죽는다" — 3바퀴 충전 ≈7.5s가
  고무줄 여유 3.3s보다 길고, 회전하면 x가 앵커 뒤로도 간다. 찬스 잎에 걸리는 순간 폭풍이 앵커 기준 450px
  (`threat.chanceBackPx`) 뒤로 물러나고, 매달려 있는 동안(`sonic.chance`) 전진·잡힘 판정을 멈춘다. 놓으면 재개.
  "SUPER!" 멈춤 연출과 같은 문법 — 찬스는 폭풍을 잠시 쫓는 안전지대. 매달려서 얻는 거리는 없으므로 점수 악용 없음
- 보류: 위협 효과음(Phase 1 효과음과 함께) · 계절별 폭풍 외형(겨울 눈보라 등) · 시작 화면 데모에 폭풍 등장 ·
  폭풍이 물러나는 연출(지금은 위치만 이동)

## D-014. 점수 표기는 미터 대신 일수 — 사계절 1000m = 365일 (2026-08-30, BUILD 20)

- 사용자 결정: "미터 대신 일수로 표기. 250m가 한 계절이니 365일을 4등분". 환산 `일수 = floor(미터 × 365 / (stepM×4))`
  → 계절 하나 250m = 91일, 1일 ≈ 2.74m. `core/season.ts daysOf·dayLabel`
- 표시 전용: HUD 거리 칩 · 결과 카드(카운트업 포함) · 최고 기록 칩 · "최고 N일" 필. **저장(best)·계측(score)·
  score_band는 미터 그대로** — 기존 기록 호환, 환산 규칙이 바뀌어도 기록이 깨지지 않는다
- 보류: 365일 넘으면 "1년 12일"식 표기 (지금은 "377일"로 누적) · 시작 화면 데모 문구

## D-015. 찬스는 "잡는 순서"로 터진다 — 계절마다 한 번, k번째 잡기 (2026-08-30, BUILD 23)

- 배경: 벽괴물(D-013) 이후 사용자 "찬스를 못 잡고 넘어가는 케이스가 많다. 전엔 뒤로 한 칸 가서 잡았는데".
  원인은 찬스 위치가 랜덤인 데다 `selectTarget`이 찬스를 안 보고 선호점 최근접만 골라 **잡히느냐도 운**이었던 것 —
  뒤돌아가기가 그 허술함을 가리고 있었다. 사용자 제안 "내가 잡는 앵커 중 랜덤"을 채택
- 규칙: 계절 단계마다 k = 시드 랜덤 [3, 10] (`sonic.chanceGrabMin/Max`). 그 계절에서 **k번째로 잡는 서로 다른
  앵커**에 로프가 걸리는 순간 찬스(히트스톱 + "SUPER!" + 금빛 잎). 같은 앵커 재잡기는 안 센다. 놓으면 소모(성공·
  실패 무관). 그 계절에서 k개를 못 잡으면(대시로 건너뜀) 소멸
- 매 앵커 확률 p 방식은 피했다: 판마다 찬스 수가 들쭉날쭉해 최고 기록이 뽑기가 되고(D-001 "내 탓"의 뒤집힌 버전),
  짧게 잡았다 놓기로 주사위를 굴리는 낚시가 생긴다. k번째 방식은 계절당 정확히 한 번·시드 결정론 유지
- 난이도는 게이지로 이동: `sweetHalf` 0.08→0.06 (성공 구간 114→86ms, 0.7Hz 삼각파). 실패 비용(7.5초 + 계절 찬스
  소멸)이 이미 커서 더 좁히기보다 필요하면 `gaugeHz`를 올린다
- 대시 200m(B22)와 합치면 봇은 90초에 1600~1900m(계절마다 대시) — 봇은 게이지를 항상 맞추므로 상한. 사람은 게이지
  성공률·매달림으로 갈린다. k [2,6]은 매 계절 시작 직후 터져 사슬이 됐고, [3,10]도 거리는 비슷하나 계절 안에 흩어진다
- D-004("확률·아이템 없음")와의 관계: 유상 구매와 무관한 무료 연출이라 게임산업법 확률형 아이템 대상이 아니다.
  내용설명서에는 "회전으로 충전하는 부스트 — 계절마다 한 번, 발동 앵커는 랜덤"으로 기재
- 제거: 앞에 보이던 금색 찬스 잎(위치 랜덤), `sonicChanceAnchor`/`isChanceAnchor`/`chanceFrom/To`. 잎은 잡는 순간 금빛이 된다
- 용어: 문서의 "폭풍"(D-013)은 **벽괴물**과 같은 것 — 그린 것이 눈·촉수라 사용자 표현대로 벽괴물로 부른다

## D-016. 슈퍼 찬스는 멈춘 채 기다리고, 다시 누르면 도전한다 — 게이지 구간은 랜덤 (2026-08-30, BUILD 24)

- 사용자: "슈퍼가 걸렸는데 나도 모르게 손을 떼면 못 쓰게 된다 — 한번 멈추게 한 다음 사용자 액션으로 도전하게".
  찬스가 랜덤 앵커에서 터지는 D-015 이후 예상 못 한 채 습관대로 놓아 찬스를 날리는 일이 늘었다
- 규칙: 찬스 앵커에 걸리면 히트스톱(0.45s) → **도전 대기(`sonic.pending`)**: 세상이 멈춘 채, 놓아도 찬스 보존, 히트스톱
  중 누름은 무시(놀라서 두드린 손가락 방지). **떼었다 다시 누르면** 도전 시작(회전 0부터). 그때부터는 기존 규칙(놓으면
  게이지 결과). 건너뛰려면 누르자마자 놓으면 된다(찬스 소모). 벽괴물은 chance 중 정지라 무기한 대기와 충돌 없음
- 매뉴얼 카드: 처음 `sonic.manualShows`(2)번은 카드(데모 애니메이션: 누른 채 3바퀴 → 노란 구간에서 떼기 → 대시 +
  "떼었다 다시 누르면 도전!" + "다시 안 보기" 체크). 그 뒤엔 배지만. 저장 `jgh.v1.super-manual` {shown, hide}
- 게이지 성공 구간 중심을 찬스마다 시드 랜덤 0.2~0.8로 (사용자 "항상 고정 위치면 더 쉽다"). 양 끝 근처는 마커가
  방향을 바꾸며 두 번 스쳐 난이도가 달라지므로 제외. 폭(sweetHalf 0.06)은 그대로
- 함께: 일수 표기 365일 이상은 "Y년 D일"(사용자 "366일이 아니라 1년 1일") · 아이폰 더블탭 돋보기 차단(플레이북 §9)
- **BUILD 26 다듬기** (사용자 "떼었다가 누르면 도전, 좀 이상하다 / 데모가 안 나온다"): 도전 시작은 그냥 **꾹 누름**.
  한 바퀴도 못 돌고 놓으면 대기로 돌아간다(찬스 보존 — 실수 탭 보호), **한 바퀴 뒤에 놓으면 포기**(찬스 소모, 건너뛰는 길은
  남긴다). 매뉴얼 카드는 "다시 안 보기"를 누르기 전까지 항상(`manualShows` 1e9) — 2회 제한은 데모가 사라진 것으로 읽혔다.
  문구 "꾹 누르면 도전!" / 데모 "꾹 누른 채 3바퀴 → 노란 구간에서 떼기! → 슈퍼 대시!"
- **게이지 입력 유예 (BUILD 26)**: 사용자 "구간 안쪽인데 경계에 살짝 걸쳤다고 실패". 판정 시점의 마커는 화면에 보이던
  마커(지난 프레임 ≤16ms)보다 터치 지연(iOS 50~80ms)만큼 앞서 있다 — 0.7Hz 삼각파는 초당 1.4칸이라 0.07~0.13칸, 구간 폭
  ±0.06과 맞먹는다. 놓기 직전 `inputGraceSec` 0.09초 안에 마커가 구간에 있었으면 성공(세 시점 샘플). 리듬게임의 판정
  윈도 보상과 같은 원리 — 반응 속도는 그대로 게임이고, 표시-판정 어긋남만 없앤다

## D-017. 슈퍼 대시는 1년(1000m)에 한 번 — 발동 방식은 그대로 (2026-08-31, BUILD 27)

- 사용자 "너무 쉬운 것 아닌가". 봇 계측(2026-08-31)이 뒷받침했다: 기존 `sim-threat` 봇은 **슈퍼를 끄고** 재고
  있어 41초로 목표 안(D-001 4번)처럼 보였으나, 찬스마다 게이지를 맞추는 봇으로 재니 **평균 141초·2868m·대시 11.5회**.
  **거리의 77%가 대시**였다 — 대시 200m가 계절 구간 250m에 맞먹어 "잡기 몇 번 → 회전 → 대시 → 다음 계절"이
  자가발전 사슬이 됐다 (B22의 dashMeters 150→200이 D-015로 끊었던 사슬을 되살린 것)
- 검토했으나 채택하지 않은 안: (1) `dashMeters` 200→130 — 사용자 "로프 몇 번 넘어가면 갈 거리라 매리트가 없다".
  3바퀴 충전(7.5초) + 게이지 위험의 대가로는 작다 (2) 금색 찬스 잎 복귀(위치로 획득 난이도 부여) — 논의만 하고 보류
- **결정: 찬스 단계를 계절(250m) → 1년(사계절 한 바퀴, 1000m)으로.** 발동 방식은 그대로 — 그 단계에서
  내가 잡는 k번째 앵커(`sonic.chanceStepM` 1000, `chanceStageOfX`). 보상 200m·게이지·도전 대기 모두 유지
- k 재조정: 봇 계측으로 1년당 잡기가 95~135회 → [3,10]은 매년 시작 직후라 [12,50]. 찬스가 그 해 앞~중반에
  흩어지고 남은 절반은 순수 스윙 구간이 된다
- 결과(봇, 180초·시드 8): 대시 11.5→2.1회, 세션 141→109~117초, 2868→2013m, 대시 기여도 77%→21%,
  사망 3/8→6~8/8. 봇은 게이지를 항상 맞추는 상한이라 사람 세션은 이보다 짧다 — 실기기 판정 필요
- 계측 회귀 방지: `sim-threat.test.ts`에 **슈퍼 켠 봇** 표를 정식 추가(`runSuperBot`). 슈퍼를 끈 표만 보면
  세션 길이를 3배 과소평가한다. 첫 테스트가 `loopsToArm=999`를 되돌리지 않아 뒤 테스트를 오염시키던 것도 수정

## D-018. 찬스는 그 해의 **랜덤한 지점**에서 열린다 — "k번째 잡기" 폐기 (2026-08-31, BUILD 27)

- 사용자 "슈퍼 대시가 매년 시작이 아니라 1년 중 랜덤으로 나와야 한다". D-017의 k=[12,50]번째 잡기는
  1년당 잡기가 95~135회라 발동이 늘 그 해 앞부분에 몰렸다. k를 넓히면(예 [8,110]) 잡기 빈도가 사람마다
  달라 어떤 해엔 아예 안 터진다 — 횟수는 위치의 나쁜 대리 변수다
- 규칙: 단계(1년)마다 시드로 정해지는 비율 f = [`chanceFrom` 0.1, `chanceTo` 0.85]. 그 지점(`sonicChanceAtPx`)을
  지나 **내가 처음 잡는 앵커**가 찬스 앵커. 플레이어가 보는 경험은 그대로다(예고 없이 터짐 → 히트스톱 →
  도전 대기 → 꾹 누르면 도전, D-016)
- 옛 금색 잎(B15~22)과 다른 점: 특정 잎을 지정하지 않는다. 어느 앵커를 잡든 지점만 지나면 터지므로
  "그 잎이 잡히느냐"가 운이 되지 않는다 (D-015가 지적한 실패 원인). 같은 앵커 재잡기 낚시도 무의미
- 첫 해만 [0.1, `firstChanceTo` 0.3] = 100~300m. 1년 1회(D-017)에 0.85까지 열어두면 사람의 평소 기록
  (300~700m)에서 슈퍼를 한 번도 못 보고 끝나는 판이 흔해지고, 매뉴얼 카드조차 안 뜬다
- **함께 고친 것 — 랜덤이 랜덤이 아니었다**: `Rng`는 LCG라 첫 출력이 시드에 거의 선형이다. `seed ^ 상수`로
  파생하던 찬스 지점은 **12개 시드 전부 148m**로 나왔고, 게이지 성공 구간 중심(D-016)도 같은 방식이라
  "찬스마다 랜덤"이 사실상 고정이었다 → 둘 다 `stageSeed()`(이미 있던 해시 믹서)로 교체.
  이제 1년차 185~280m, 이후 연차는 121~834m로 흩어진다
- 결과(봇 180초·시드 8, 게이지 항상 성공): 대시 1.9~2.0회, 세션 102~118초, 1900~2038m — D-017과 같은
  수준이되 발동 위치만 흩어졌다

## D-019. 밸런스 점검 — 이어하기 재출발은 다음 잎 기준, 첫 해 찬스 50~200m, 상위권 상한은 보류 (2026-09-02, BUILD 32)

- 사용자 "게임 밸런스가 전체적으로 잘 배분되어 있는지 점검" → 슈퍼 켠 봇으로 **분포**(중앙값·사분위)와 이어하기 뒤
  생존 시간을 쟀다. 평소 플레이어(θ=35°, 기록 300~700m)의 첫 생 중앙값 41~47초로 D-001 4번(30~60초) 안. 이어하기 한 번의
  가치는 첫 생의 60~80%. 게이지 성공 여부가 거리에 거의 영향 없음(100% 맞춤 640m vs 0% 585m) — 대시는 경제가 아니라
  연출로 자리 잡았다(D-017 목표 달성). 문제는 양 끝이었다
- **이어하기 직후 조기 사망 (수정)**: θ=35°에서 이어하기 뒤 5초 안 사망이 119번 중 11번. 위협 머리 여유를 시간 기준으로
  늘려도, 4초 얼려도 안 줄었다 → 프레임 추적으로 원인 확인: 재출발이 **마지막 잎 기준(+75,+130)**이라 후반처럼 잎 높이가
  ±175px 흔들리는 구간에서 **다음 잎이 플레이어보다 낮게** 놓인다. 잡을 게 없어 떨어지다 코앞 잎을 31~94px 로프로 잡고
  제자리 회전하다 잡힘. 게임 시작은 첫 잎이 150 앞·130 위로 고정돼 이 문제가 없었다
  → **재출발: x는 죽은 자리(다음 잎 150 앞을 넘지 않게), y는 다음 잎 130 아래.** 죽음이 거리를 주지 않고(x ≤ 죽은 자리),
  첫 잡기가 게임 시작과 같은 기하가 된다. 5초 안 사망 11 → 1/119. 속도(520, −300)는 그대로 — 시작 속도(380, −120)와
  비교해 차이 없었다. 다음 잎 기준으로 x까지 옮기는 안(A/C)은 거리가 최대 4.7m 늘어 제외
- **첫 해 찬스 앞당김 (수정)**: 초보 봇(θ=45°, 20~27초 사망)의 절반이 찬스(100~300m) 전에 죽어 슈퍼도 매뉴얼 카드도 못 봤다.
  `chanceFrom` 0.1→0.05, `firstChanceTo` 0.3→0.2 (첫 해 50~200m). 45° 봇이 찬스를 보는 비율 12→17/24, 55° 4→7/24.
  평균 플레이어 세션은 그대로(41→46초). 2년차 이후 구간은 [0.05, 0.85]
- **상위권 상한 (보류 — 기존 노브로는 불가)**: 상위 봇(θ=25°) 첫 생 중앙값 119~125초, 300초 생존 1~2/24. 고무줄을 연차마다
  500까지 줄여도, 위협 램프를 +40→+100/년(cap 800)으로 올려도 **변화 없음**. 이유: 고무줄 때문에 위협은 늘 1000px 뒤에
  붙어 있고 죽음은 스톨(코앞 잎 제자리 회전)에서만 나므로 위협 속도가 무관하다. 스톨 빈도는 잎 배치가 정하는데 간격 상한
  390×1.15=449는 이미 사거리 450에 닿아 있다 — 더 벌리면 닿을 수 없는 배치("내 탓 아닌 죽음", D-001 3번). **상위권 세션을
  줄이려면 새 메커니즘이 필요**(사거리 연차 축소는 D-013에서 억울함 비용으로 제외, 잎 찢어짐 타이머 등). 실측 세션 분포에서
  상위 10%가 실제로 얼마인지 본 뒤 결정. 개발자 감("너무 쉽다")으로 조이지 않는다(HANDOFF 함정)
- 계측 도구: `sim-threat.test.ts`에 **이어하기 봇 표** 추가(`runContinueBot`, 게이지 50%·이어하기 2회·이어하기 뒤 <5s/<10s
  사망 수). 세 표(위협 유무·슈퍼 켬·이어하기)가 이제 난이도의 기준

## D-020. 이름: 날아날아 (naranara) — D-002 정글훅을 대체 (2026-09-03, BUILD 32)

사용자: "이름을 좀 쉬운 걸로. 발음하기 쉬운 두 글자 단어가 반복되는 구조." 후보를 동작별로
넓게 뽑아(매달림·날아감·잡기·정글·전진·일수) D-002와 같은 기준으로 동명 검색 뒤 확정.

- **날아날아** — 릴리스 순간(관성 발사)이 이름. 이 게임의 손맛이자 리텐션 보험(D-001)인 순간을
  앞세운다. 이어하기 문구 "한 번 더 날아"로 이어진다. 동명 게임·앱 없음, `naranara`도 동명 없음
- 탈락한 유력 후보와 이유:
  - **하루하루** — 앵커 하나가 대략 하루 이틀(gap 175~300px ÷ 50px/m, 1000m=365일 D-014)이라 뜻은
    가장 좋았으나 동명 앱이 다수(일기·자기관리·만남·습관). 게임은 아니지만 스윙팡을 뗀 D-002 기준
    (비게임이라도 동명 앱 보유)에 걸린다. 변형 내일내일·하루더는 깨끗했으나 사용자가 날아날아 선택
  - **대롱대롱·달랑달랑·둥실둥실·나풀나풀** — 모두 깨끗. 어감 취향으로 미선택
  - **슝슝**(방치형 RPG) · **씽씽**(킥보드) · **붕붕**(다함께 붕붕붕) · **휘리릭**(용사들) · **빙글빙글**
    (돌림판·스팀) · **영차영차**(햄찌는 영차영차) · **당겨당겨**(텐텐 오락실 미니게임) · **타잔타잔**(디즈니) — 동명 존재
  - 와리가리 — 기존 사용 과다 + 일본어 유래 어감
- **슬러그까지 바꾼다.** 출시 전이라 잃는 게 없고 출시 뒤엔 영영 못 바꾼다. appName `naranara` ·
  딥링크 `intoss://naranara` · 저장 접두어 `nara.v1` · 이벤트 접두어 `nara_` · 개발 훅 `__nara`.
  옛 `jgh.v1` 값(테스트 폰의 최고기록·음소거·매뉴얼 표시 상태)은 마이그레이션 없이 버린다 — 출시 전
  테스트 데이터뿐이다. 리포명도 같은 날 `jsypsy/naranara`로 바꿨다(처음엔 junglehook 유지로 적었으나 GitHub에서 리포명 변경 + 로컬 새 클론)
- 콘솔: `junglehook` 미니앱 등록·테스트 배포(B31)는 무효. **`naranara`로 새로 등록**하고 다시 배포한다
- ⚠️ D-002의 GRAC 제명 재확인 원칙은 그대로 — 신청 직전 등급분류 검색으로 "날아날아" 동명 재확인

## D-021. 효과음은 최소 5종으로 시작 — 잡기·놓기·찬스·게이지 성공/실패·사망 (2026-09-03, BUILD 37)

- 사용자 "효과음 없는 게임도 있나?" → 있지만 소수이고(2048·Wordle류), 타이밍이 핵심인 원버튼 게임(플래피버드·스틱 히어로)은
  거의 전부 소리가 있다. 무음 출시도 가능하지만(판정 정보가 전부 시각으로 나가 있다) 이 게임의 리텐션 보험이 스윙의 손맛
  (D-001)이라 놓는 순간의 소리가 그 절반이다 → **최소 5개만** 넣고 나머지(펌프 속도 연동음·로켓 지속음·벽괴물 경고음·신기록)는
  보류. 사용자 결정 "최소 5개로"
- 구조: core는 소리를 모른다 — `Game.events`에 `grab/release/chance/dash/dashFail/die`를 남기고 main이 매 틱 비운다.
  허공에서 놓기·도전 대기 중 놓기는 이벤트 없음(로프를 쥔 적이 없거나 아무 일도 안 일어난다)
- 음색: 한줄팡 검증 뼈대(제스처 안 AudioContext·시계 확인·백그라운드 정지) 위에 WebAudio 합성. 슈팅스타 음색 메서드는 전부 제거
- 심사 요건: 리워드 광고 전후 `sound.setMuted` 강제(플레이북 §3). 소리 토글 칩은 시작 화면·결과 카드 왼쪽 아래(`nara.v1.muted`
  저장). 플레이 중에는 토글이 없다 — 화면 전체가 버튼이라(D-005) 토글이 있으면 오탭이 된다
- GRAC: 설명서 5.6항에 5종 표로 명시, 장면 14(소리 끄기) 추가. 비명·타격음 없음
- **추가 (BUILD 38, 같은 날)**: 사용자 "뱅글뱅글 돌 때 윙윙윙 효과음 되려나?" → 보류 목록의 "회전 충전"을 넣었다. 단발 `tone`과 달리
  **지속 보이스**(`spin`/`spinStop`: 톱니파 → 로우패스 → 게인, 매 프레임 setTargetAtTime). 찬스 잎에 매달려 도는 동안만 —
  보통 스윙에서 울리면 1분 내내 피로하고 찬스의 신호가 사라진다. 음량은 앵커 기준 각도로 아래를 지날 때 부풀어 한 바퀴에 "윙" 한 번,
  음정은 바퀴마다 장3도(110→139→175→220Hz), 느리면 작게. 도전 대기·히트스톱·놓기·잡힘·광고·백그라운드에서 즉시 정지.
  시계가 흐르기 전엔 시작하지 않고 보관도 하지 않는다(놓치면 다음 프레임). 효과음은 6종

## D-022. 효과음 전부 제거 — 무음으로 간다 (2026-09-03, BUILD 39, D-021 폐기)

- 사용자 결정 "사운드는 그냥 다 없애자". B37(5종)·B38(회전 충전 윙윙윙)을 폰에서 들어본 뒤의 판단.
  코드는 소리 이전 상태(B36 합류 시점 `da5a513`의 `src/`)로 되돌리고 `sound.ts`(한줄팡 뼈대)도 지웠다 — 필요하면 git 이력
  (`3a2de61`·`5691b19`)에 그대로 있다. `Game.events`·소리 토글 칩·광고 mute 배선도 함께 제거
- GRAC 설명서는 다시 "무음"으로(5.6항·13항·00 개요·02 3항), 장면 14(소리 끄기) 삭제. 무음은 등급·심사에 영향 없다.
  출시 뒤 소리를 넣는 것은 내용수정신고 대상이 아니다(플레이북 §4 표현·규칙 무변경)
- 되살릴 때 참고: 이벤트 큐 방식(core는 사실만 남기고 main이 재생)과 지속음의 정지 경로(놓기·잡힘·광고·mute·백그라운드)는
  D-021에 정리돼 있다
