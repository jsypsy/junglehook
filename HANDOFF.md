# Session Handoff
_Last updated: 2026-08-31 (BUILD 26)_

## 지금 상태

**정글훅 — 카툰 정글 + 계절 나무 + 벽괴물 + 일수 표기 + 랜덤 찬스 슈퍼 모드 (BUILD 26). B19~B26 실기기 판정 대기.**

```
컨셉       원버튼 그래플 스윙 (홀드=로프+감기+펌프, 릴리스=관성 발사). D-001, D-005, D-006
이름       정글훅 / junglehook — 충돌 검색 완료. GRAC 제명은 신청 직전 재확인 (D-002)
경로       /Users/jsypsy/Documents/workspace/toss/junglehook (2026-08-30 이동). 형제 앱: toss/hanjulpang(출시) · toss/shootingstar(검수 단계)
코드       BUILD 26 — 슈퍼 도전 "꾹 누르면"(한 바퀴 전 놓으면 대기, 뒤면 포기) · 게이지 입력 유예 90ms · 매뉴얼 카드 항상 · 나무 스프라이트 경계 수정 · 겨울 팔레트 추운 색(흐린 파랑·갈색 가지)
           B25 계절 나무(render/tree.ts: 가지 뼈대+잎 밀도, 스프라이트 캐시) · 태양 후광 여름만
           B24 슈퍼 도전 대기(찬스 → 멈춘 채 → 누르면 도전) + 매뉴얼 카드 2회·"다시 안 보기" + 게이지 구간 랜덤 0.2~0.8 (D-016)
           B23 찬스 = 계절마다 k번째 잡기(k 3~10 시드 랜덤), 게이지 0.06 (D-015) · B22 대시 200m=73일
           B20 일수 표기 1000m=365일, "1년 1일" (D-014) · B19~21 벽괴물(D-013: 300→420px/s, 고무줄 1000, 찬스 중 후퇴·정지)
           그 아래: 슈퍼 모드(D-012) · 계절 표정 · 사계절 2차 램프 · 사계절 250m(D-010) · 이어하기 광고 루프(D-009) · 카툰(D-008)
           테스트 54개(+계측 2개 skip). iOS 더블탭 돋보기 차단(플레이북 §9)
실플레이   B21에서 278일(762m, B18 최고 1115m의 68%) "많이 어려워졌다" → B22~24로 보상·찬스·도전 대기 조정. B22 이후는 미판정
콘솔       junglehook 등록됨 · 테스트 배포 최신 BUILD 26 — tools/latest-deployment.json 
디자인     밝은 카툰 정글(D-008) + 사계절(D-010) + 캐노피(D-011) + 계절 나무(design/trees, 렌더러 반영 완료) — `design/`
다음       **B25 실기기 판정** → 피드백 반영 → Phase 1 잔여(효과음·점수 제출·뒤로가기 모달)
```

## 다음 세션이 할 일

0. **B26 실기기 판정** (배포·문자 전송 완료). B26: 문구·규칙(꾹 누르면 도전, 한 바퀴 기준) · 게이지 입력 유예(`sonic.inputGraceSec` 0.09 — 여전히 억울하면 0.12) · 카드 항상 · 나무 경계. 볼 것과 노브:
   - **계절 나무**: 폰 프레임이 매끄러운가(맥 DPR2 2.4~3.9ms/프레임) — 떨어지면 `renderer.drawTreeLayer` 먼 나무 4→3그루,
     또는 `tree.ts drawTreeSprite`의 scale. 겨울 앙상함·여름 풍성함 체감, 봄 0.45·가을 0.75 밀도(`renderer LEAF_DENSITY`)
   - **슈퍼 도전 대기(D-016)**: 찬스 → 멈춤 → 꾹 누름이 자연스러운가, 한 바퀴 기준 포기 규칙이 납득되는가, 데모 카드가 매번 떠도
     성가시지 않은가("다시 안 보기"로 끄는 흐름)
   - **찬스·게이지**: 계절마다 찬스가 터지는가(k `sonic.chanceGrabMin/Max` 3~10), 게이지 86ms 성공률 "3번에 2번"쯤인가 —
     너무 어려우면 `sweetHalf` 0.07, 더 어렵게는 `gaugeHz` 0.85. 구간 위치 랜덤이 체감되는가
   - **벽괴물**: 한 판 30~60초인가, 잡힘이 "내가 지체했다"로 납득되는가(경고 어둠·쉐브론이 미리 보이는가). 어려우면
     `threat.maxLeadPx` 1000→1200 → `speedMin` 300→270 → `speedPerCycle` 40→25 순. 봇 재계측:
     `SIM=1 npx vitest run src/core/sim-threat.test.ts --disable-console-intercept`
   - **iOS 더블탭 돋보기**가 사라졌는가 — 같은 패치가 한줄팡·슈팅스타에도 들어갔으니 여기서 먼저 확인
   - 태양이 봄·가을·겨울에 작아 보이는가(후광은 여름만) · 겨울이 "흑백"이 아니라 "추운 흐린 날"로 읽히는가(`SEASON_PALETTE.winter`)
1. **한줄팡 검수 요청** (다른 저장소 `toss/hanjulpang`): 더블탭 수정 테스트 배포 `01a0532c…`까지 됐고 검수 요청은 콘솔 MCP
   (`bundle_submit_review`)가 세션에 로드돼 있어야 한다 — 2026-08-30 세션엔 안 붙어 있어 못 했다. 정글훅에서 돋보기 수정을
   확인한 뒤 hanjulpang 세션에서 진행. 슈팅스타는 검수 단계라 테스트 배포(BUILD 30)만
2. 보류 연출: 벽괴물이 찬스 때 물러나는 애니메이션(지금은 위치 순간이동) · 시작 화면 데모에 벽괴물 등장 · 계절별 벽괴물 외형
3. Phase 1 잔여: 효과음 교체(sound.ts, 벽괴물·찬스 소리 포함, 광고 중 mute) · 점수 제출(플레이 완료 후) · 뒤로가기 확인 모달 ·
   내용설명서에 "이어하기 광고"·"회전으로 충전하는 부스트(계절마다 한 번, 발동 앵커는 랜덤)" 기재(D-004·D-015)
4. 검토: 매달린 동안 "재누름 시 잡힐 앵커" 링 표시 (D-006 탈출로 가시화)

## 작업 도구

- 크롬 MCP 검수: `/tools/frame.html`(393×749) + 개발 훅 `window.__jgh`(game·tuning·preset·meters·stepOnce). 탭이 hidden이면
  rAF가 멈추므로 `__jgh.stepOnce(dt)`로 틱을 돌린다. **순간이동 캡처 함정** (2026-08-30 두 번 데임): ① 매달린 몸은
  `g.body.anchor = null`로 풀고 옮겨야 한다(강체 로프가 제자리로 되돌린다) ② 순간이동 잡기가 "k번째 잡기"로 세어져 찬스가 터지고
  세상이 멈추므로 `tuning.sonic.chanceGrabMin/Max = 9999` ③ 저장값(매뉴얼 hide 등)은 모듈 로드 때 읽으므로 localStorage를
  바꾼 뒤엔 iframe reload. 사계절은 게임 캔버스를 오프스크린 스트립에 이어 붙여 한 장으로 본다
- 봇 계측: `src/core/sim-threat.test.ts`(SIM=1) — 봇은 도전 대기(pending)면 떼었다 다시 누른다
- 폰 접속: 아이폰 핫스팟이 IPv6 전용이면 맥이 192.0.0.2를 받아 접속 불가 → 맥의 IPv6 글로벌 주소 `http://[…]:5173/`로
- 디자인 캔버스: `design/trees/gen.py`처럼 파이썬으로 보드를 생성 → `/design` 스킬로 게시. 링크는 design/README.md

## 잊지 말 것

- 설계 변경은 D-001의 "수익 역산 원칙 5"에 비춰본다
- 랜덤은 연출·배치·발동 순서에만(D-015) — 보상 확률·유상 요소 없음(D-004). 내용설명서에 랜덤 발동 기재
- 배포는 `ait:build → ait:deploy → ait:send` 한 묶음 — URL을 연락처 "정성엽"(본인 폰)에게 아이메시지로 보내야 폰에서 확인
  가능 (수신자는 tools/imessage.local.json, gitignore). 한줄팡·슈팅스타엔 ait:send가 없어 이 수신자로 osascript 직접 전송
- 게임오버 문구는 "끝" (사용자 결정). 사망 연출은 피·붉은색 없이 — GRAC 전체이용가. 벽괴물도 잉크·노란 눈뿐
- 문서의 "폭풍"(D-013 초기 표기)은 벽괴물과 같은 것 — 사용자 표현은 벽괴물
- 점수 표시는 일수지만 저장(best)·계측(score)은 미터 그대로 (D-014) — 환산 규칙이 바뀌어도 기록이 안 깨진다
- 클라우드 세션은 콘솔 인증·브라우저 없음 → 배포·실기기 확인은 로컬에서. 콘솔 MCP는 세션 시작 때 붙어 있어야 쓸 수 있다
- sound.ts 효과음 메서드는 슈팅스타 것 — 게임 구현 때 교체
