# Session Handoff
_Last updated: 2026-09-03 (BUILD 36)_

## 지금 상태

**날아날아 — 두 갈래 합류 (BUILD 36): 이름 변경(naranara 갈래) + 밸런스 점검 B32·데모 손가락 B33·태양 획 B34·B35(junglehook 갈래).
콘솔 `naranara` 등록 완료, **B36 미배포**. GRAC 제출 초안 작성됨(`grac-제출/`). 효과음은 아직.**

```
컨셉       원버튼 그래플 스윙 (홀드=로프+감기+펌프, 릴리스=관성 발사). D-001, D-005, D-006
이름       **날아날아 / naranara** (D-020, 2026-09-03) — 동명 검색 완료. 리포명도 naranara. GRAC 제명은 신청 직전 재확인
경로       /Users/jsypsy/Documents/workspace/toss/naranara. 형제 앱: toss/hanjulpang(출시) · toss/shootingstar(검수 단계)
코드       BUILD 36 — 두 갈래 합류(아래 B32~35 + 이름 변경). 이름 변경: appName `naranara` · `intoss://naranara` · 저장 `nara.v1`
           · 계측 `nara_` · 개발 훅 `__nara` (D-020)
           B35 — 태양 외곽선 흔들림 1.3u→0.45u (감자처럼 보였다)
           B34 — 태양: 여름 광선을 크레용 획(주황+노랑)으로, 외곽선은 손으로 두 번 돌린 선 (drawBackground)
           B33 — 데모 손가락: 누르면 손끝 쪽(위)으로 밀고 링은 손끝에 (아래로 내려 누름·뗌이 반대로 보였다)
           B32 — 밸런스 점검(D-019): 이어하기 재출발을 **다음 잎 130 아래·죽은 자리 x**로(후반에 다음 잎이 아래에
           놓여 5초 안 사망 11/119 → 1) · 첫 해 찬스 100~300m→**50~200m**(초보가 슈퍼를 못 보고 죽음) · 상위권 상한은
           기존 노브로 불가(고무줄·위협 속도 무효, 잎 간격은 사거리 한계) → 보류. 이어하기 봇 표 추가
           B31 — 잎이 가을 내내 지고 겨울 초입에 마저 떨어짐(`leafDensityAt`) · 대시 화염을 **획으로 긋는**
           방식(`drawFlameFan`) · 스파크(번개 아크) 제거 · HUD 두 줄 블록의 세로 중앙 = 태양 중심, 둘 다 위로
           · 시작 데모 문구 "다음 앵커에"→"다음 잎으로"
           B30 iOS 브라우저에서 캔버스를 시각 뷰포트에 맞춰 붙임(주소창 뒤로 밀리던 것)
           B29 최고기록 칩을 거리 칩 아래로(태양과 매 여름 겹쳤다)
           B27 슈퍼 대시 1년(1000m) 1회(D-017) · 발동은 그 해의 랜덤 지점 뒤 첫 잡기(D-018, k번째 잡기 폐기)
           · LCG 시드 파생 버그 수정(찬스 지점·게이지 구간 중심이 사실상 고정이었다) · 매뉴얼 카드가 영구히 사라지던 버그
           B26 슈퍼 도전 "꾹 누르면" · 게이지 입력 유예 90ms · 겨울 팔레트 추운 색
           B25 계절 나무(render/tree.ts) · B24 도전 대기 + 매뉴얼 카드 + 게이지 구간 랜덤 (D-016)
           B23 찬스 게이지 0.06 (D-015) · B22 대시 200m=73일 · B20 일수 표기 1000m=365일 (D-014)
           B19~21 벽괴물(D-013) · 그 아래: 슈퍼 모드(D-012) · 사계절 250m(D-010) · 이어하기 광고(D-009) · 카툰(D-008)
           테스트 54개(+계측 3개 skip). iOS 더블탭 돋보기 차단(플레이북 §9)
실플레이   B32 봇(슈퍼 켬): θ=35° 66초·988m, 30° 68초, 25° 81초. 이어하기 뒤 중앙값 34초. 실기기 판정은 사용자가 건너뜀(2026-09-02)
           사용자 "너무 쉬운 것 아닌가"(B26) → 봇 계측이 뒷받침: 슈퍼 켠 봇 141초·2868m·대시 11.5회, 거리의 77%가 대시
           → B27로 1년 1회. 재계측 102~118초·1900~2038m·대시 1.9~2.0회 (봇은 게이지를 항상 맞추는 상한).
           **난이도는 아직 실기기 미판정** — B29~31은 로컬 개발서버로만 봤다
콘솔       naranara 등록 완료(2026-09-03) · **B36 미배포**. junglehook 슬러그로 나간 B31~B35 배포(최신 `01a062a9…`)는 옛 슬러그라 무효
저장소     origin = github.com/jsypsy/naranara (리포명 변경 2026-09-03). `toss/junglehook` 로컬 폴더는 합류했으니 더 쓰지 않는다.
           디자인 캔버스 **게시본은 커밋 안 함**(2MB대라 웹 세션 푸시가 막힌다)
디자인     밝은 카툰 정글(D-008) + 사계절(D-010) + 캐노피(D-011) + 계절 나무(design/trees) — `design/`, 링크는 design/README.md
GRAC       `grac-제출/` 초안(B32 기준, 미접수). 동결 전 체크리스트는 그 README — 효과음·프리셋 제거·gh-pages·영상이 남았다
다음       **B36 배포(naranara 슬러그)** → 실기기/계측 확인 → **효과음 추가** → Phase 1 잔여 → GRAC 동결·접수
```

## 다음 세션이 할 일

0. **B36 배포** — 콘솔 등록은 끝났다. 로컬이면 `ait:build → ait:deploy → ait:send` 한 묶음. 로컬이 없으면
   GitHub Actions "앱인토스 테스트 배포"(플레이북 §2) — 리포 시크릿 `AIT_API_KEY`가 먼저 있어야 한다.
   naranara 슬러그의 첫 빌드라 타이틀에 "날아날아"가 찍히는지, 옛 `jgh.v1` 저장값이 사라져 최고기록이 0인지 확인
1. **폰에서 볼 것** (실기기 판정은 사용자가 건너뛰기로 했으니(2026-09-02) 계측 `nara_game_over` cause·continued·세션 길이 분포로 본다):
   - 이어하기 뒤 첫 잡기가 자연스러운가 (다음 잎이 항상 위에 있어야 한다, B32) · 첫 슈퍼가 50~200m(5~15초)에 터지는 게 너무 이른가
   - 데모 손가락 방향(B33) · 태양 획 연출·동그람(B34·B35)
   - **게이지 구간 위치가 매번 다른가** — 억울하면 `sweetHalf` 0.06→0.07
   - **HUD·태양**: 두 줄 블록의 세로 중앙이 태양 중심과 맞는가 (거슬리면 태양 76u→82u, HUD `top`은 41u로 함께)
   - 상위권 세션이 실제로 길면 새 메커니즘 논의(D-019 보류 항목)
2. **효과음 추가 (사용자 지정 — 다음 작업)**. `src/sound.ts`의 기동·복구 뼈대(한줄팡 D-009~D-012 산물)는 그대로 쓰고
   **효과음 메서드는 슈팅스타 것이라 전부 교체**한다. 런타임 의존성 추가 금지 → WebAudio로 직접 합성.
   - 필요한 소리: 로프 잡기 / 릴리스 · 스윙 펌프(속도 연동) · **찬스 발동**(히트스톱과 붙는 알림) · 회전 충전(바퀴마다 음정↑)
     · 게이지 성공(대시 발사) / 실패 · 대시 중 로켓 지속음 · 벽괴물 접근 경고(거리 연동) · 사망 · 신기록
   - 심사 요건: **리워드 광고 재생 중 게임 오디오 직접 mute**(플레이북 §3) — 광고 전후로 `sound` 뮤트/복구를 반드시 태운다
   - 음소거 상태는 `nara.v1.muted`에 이미 저장된다. 소리 추가 후 BUILD +1
3. **한줄팡 검수 요청** (다른 저장소 `toss/hanjulpang`): 더블탭 수정 테스트 배포 `01a0532c…`까지 됐고 검수 요청은 콘솔 MCP
   (`bundle_submit_review`)가 세션에 로드돼 있어야 한다 — 2026-08-30 세션엔 안 붙어 있어 못 했다. 날아날아에서 돋보기 수정을
   확인한 뒤 hanjulpang 세션에서 진행. 슈팅스타는 검수 단계라 테스트 배포(BUILD 30)만
4. 보류 연출: 벽괴물이 찬스 때 물러나는 애니메이션(지금은 위치 순간이동) · 시작 화면 데모에 벽괴물 등장 · 계절별 벽괴물 외형
5. Phase 1 잔여(효과음은 2번으로 뺐다): 점수 제출(플레이 완료 후) · 뒤로가기 확인 모달 — 넣으면 `grac-제출/` 설명서의
   해당 절도 고친다(README 체크리스트 3번). 내용설명서 자체는 초안이 있다(이어하기 광고·슈퍼 대시·랜덤 발동 기재됨)
6. 검토: 매달린 동안 "재누름 시 잡힐 앵커" 링 표시 (D-006 탈출로 가시화)

## 작업 도구

- 크롬 MCP 검수: `/tools/frame.html`(393×749) + 개발 훅 `window.__nara`(game·tuning·preset·meters·stepOnce). 탭이 hidden이면
  rAF가 멈추므로 `__nara.stepOnce(dt)`로 틱을 돌린다. **순간이동 캡처 함정** (2026-08-30 두 번 데임): ① 매달린 몸은
  `g.body.anchor = null`로 풀고 옮겨야 한다(강체 로프가 제자리로 되돌린다) ② 순간이동으로 찬스 지점을 넘으면 찬스가 터져
  세상이 멈추므로 `tuning.sonic.chanceStepM = 1e9`으로 찬스를 아득히 밀어둔다 (B27부터, 옛 `chanceGrabMin/Max`는 없다) ③ 저장값(매뉴얼 hide 등)은 모듈 로드 때 읽으므로 localStorage를
  바꾼 뒤엔 iframe reload. 사계절은 게임 캔버스를 오프스크린 스트립에 이어 붙여 한 장으로 본다
- **계절 진행 검수**: `tools/seasons.html` — 게임을 숨겨 띄우고 구간마다 순간이동시켜 화면을 이어 붙인다.
  가을·겨울 나무는 500m 넘게 살아야 보여서 플레이로는 확인이 안 된다. 폰에서도 열린다
- 봇 계측: `src/core/sim-threat.test.ts`(SIM=1) — 표가 둘이다: 위협 유무별(슈퍼 끔)과 **슈퍼 켠 종단**(`runSuperBot`,
  세션 길이 기준표). 봇은 도전 대기(pending)면 눌러 도전하고 게이지를 맞춘다. 첫 표가 `loopsToArm`을 건드리므로 되돌리는지 확인
- 폰 접속: `npm run dev -- --host` 후 맥의 LAN 주소(2026-08-31 기준 `http://172.30.1.46:5173/`)를 문자로.
  아이폰 핫스팟이 IPv6 전용이면 맥이 192.0.0.2를 받아 접속 불가 → 맥의 IPv6 글로벌 주소 `http://[…]:5173/`로.
  브라우저로 보면 주소창 때문에 캔버스가 어긋나 보일 수 있다 — B30에서 시각 뷰포트 오프셋으로 보정했다
- 디자인 캔버스: `design/trees/gen.py`처럼 파이썬으로 보드를 생성 → `/design` 스킬로 게시. 링크는 design/README.md

## 잊지 말 것

- 설계 변경은 D-001의 "수익 역산 원칙 5"에 비춰본다
- 랜덤은 연출·배치·발동 순서에만(D-015) — 보상 확률·유상 요소 없음(D-004). 내용설명서에 랜덤 발동 기재
- 배포는 `ait:build → ait:deploy → ait:send` 한 묶음 — URL을 연락처 "정성엽"(본인 폰)에게 아이메시지로 보내야 폰에서 확인
  가능 (수신자는 tools/imessage.local.json, gitignore). 한줄팡·슈팅스타엔 ait:send가 없어 이 수신자로 osascript 직접 전송
- 게임오버 문구는 "끝" (사용자 결정). 사망 연출은 피·붉은색 없이 — GRAC 전체이용가. 벽괴물도 잉크·노란 눈뿐
- 문서의 "폭풍"(D-013 초기 표기)은 벽괴물과 같은 것 — 사용자 표현은 벽괴물
- 점수 표시는 일수지만 저장(best)·계측(score)은 미터 그대로 (D-014) — 환산 규칙이 바뀌어도 기록이 안 깨진다
- 클라우드 세션은 콘솔 인증·브라우저 없음, `apps-in-toss.toss.im`은 프록시 403 → 배포는 로컬 또는 GitHub Actions.
  콘솔 MCP는 세션 시작 때 붙어 있어야 쓸 수 있다
- sound.ts 효과음 메서드는 슈팅스타 것 — 게임 구현 때 교체
- **`Rng`는 LCG라 첫 출력이 시드에 거의 선형이다** — `new Rng(seed ^ 상수)`로 파생하면 어느 시드나 같은 값이 나온다
  (2026-08-31: 찬스 지점이 12개 시드 전부 148m). 스테이지별 랜덤은 반드시 `stageSeed(runSeed, stage)`를 거친다
- **되돌릴 수 없는 토글을 주 액션 근처에 두지 않는다** — "다시 안 보기" 히트 영역이 "꾹 누르면 도전!" 바로 아래라
  도전하려던 탭이 카드를 영구히 껐고, 카드가 없으니 되돌릴 UI도 없었다 (B26 "예시 애니메이션이 안 나온다"의 원인)
- 난이도 판정은 감이 아니라 **슈퍼 켠 봇의 세션 길이**로 — 개발자는 최상위 플레이어라 "너무 쉽다"를 그대로 따르면
  B21("많이 어려워졌다")이 재현된다
