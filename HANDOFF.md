# Session Handoff
_Last updated: 2026-08-31 (BUILD 29)_

## 지금 상태

**정글훅 — 슈퍼 대시를 1년 1회·그 해 랜덤 지점으로, 대시 화염 셋, HUD 왼쪽 스택 (BUILD 29). 실기기 판정 대기.**

```
컨셉       원버튼 그래플 스윙 (홀드=로프+감기+펌프, 릴리스=관성 발사). D-001, D-005, D-006
이름       정글훅 / junglehook — 충돌 검색 완료. GRAC 제명은 신청 직전 재확인 (D-002)
경로       /Users/jsypsy/Documents/workspace/toss/junglehook. 형제 앱: toss/hanjulpang(출시) · toss/shootingstar(검수 단계)
코드       BUILD 29 — 최고기록 칩을 거리 칩 아래로(태양과 매 여름 겹쳤다)
           B28 대시 화염을 큰 것 하나 + 작은 것 둘로 분리, 주기가 다른 애니메이션(`renderer.drawPlume`)
           B27 슈퍼 대시 1년(1000m) 1회(D-017) · 발동은 그 해의 랜덤 지점 뒤 첫 잡기(D-018, k번째 잡기 폐기)
           · LCG 시드 파생 버그 수정(찬스 지점·게이지 구간 중심이 사실상 고정이었다) · 매뉴얼 카드가 영구히 사라지던 버그
           B26 슈퍼 도전 "꾹 누르면"(한 바퀴 전 놓으면 대기, 뒤면 포기) · 게이지 입력 유예 90ms · 겨울 팔레트 추운 색
           B25 계절 나무(render/tree.ts) · 태양 후광 여름만 · B24 도전 대기 + 매뉴얼 카드 + 게이지 구간 랜덤 (D-016)
           B23 찬스 게이지 0.06 (D-015) · B22 대시 200m=73일 · B20 일수 표기 1000m=365일 (D-014)
           B19~21 벽괴물(D-013: 300→420px/s, 고무줄 1000, 찬스 중 후퇴·정지)
           그 아래: 슈퍼 모드(D-012) · 계절 표정 · 사계절 250m(D-010) · 이어하기 광고 루프(D-009) · 카툰(D-008)
           테스트 54개(+계측 3개 skip). iOS 더블탭 돋보기 차단(플레이북 §9)
실플레이   사용자 "너무 쉬운 것 아닌가"(B26) → 봇 계측이 뒷받침: 슈퍼 켠 봇 141초·2868m·대시 11.5회, 거리의 77%가 대시
           → B27로 1년 1회. 재계측 102~118초·1900~2038m·대시 1.9~2.0회 (봇은 게이지를 항상 맞추는 상한). B27 실기기 미판정
콘솔       junglehook 등록됨 · 테스트 배포 최신 BUILD 28 `01a057f4…` (2026-08-31 22:14, 문자 전송 완료) — tools/latest-deployment.json
디자인     밝은 카툰 정글(D-008) + 사계절(D-010) + 캐노피(D-011) + 계절 나무(design/trees, 렌더러 반영 완료) — `design/`
다음       **B27 실기기 판정** → **효과음 추가**(사용자 지정) → Phase 1 잔여(점수 제출·뒤로가기 모달)
```

## 다음 세션이 할 일

0. **B29 실기기 판정** (B28까지 배포·문자 전송, **B29는 미배포**). 이번 빌드의 축은 난이도다 — **한 판이 몇 초로 끝나는지**가 핵심 숫자(D-001 4번: 30~60초).
   - **슈퍼 대시 1년 1회(D-017)**: 계절마다가 아니라 1000m마다 한 번. 너무 귀해서 심심하면 `sonic.chanceStepM` 1000→750,
     반대로 여전히 쉬우면 `dashMeters` 200→170. 대시 보상 200m=73일은 유지가 사용자 결정("130m는 매리트가 없다")
   - **발동 지점(D-018)**: 그 해의 랜덤 지점(구간 10~85%)을 지나 처음 잡는 앵커. 첫 해만 100~300m(`firstChanceTo` 0.3).
     "매년 같은 데서 나온다"고 느껴지면 `chanceFrom/chanceTo`, 첫 판에서 슈퍼를 못 보고 끝나면 `firstChanceTo`를 낮춘다
   - **게이지 구간 위치가 매번 다른가** — LCG 파생 버그로 B24~26에선 사실상 고정이었다. 이제 진짜 랜덤이라
     체감 난이도가 올랐을 수 있다. 억울하면 `sweetHalf` 0.06→0.07 (유예 `inputGraceSec` 0.09는 유지)
   - **매뉴얼 카드(예시 애니메이션)가 다시 뜨는가** — B26까지 안 뜬 원인은 "다시 안 보기" 오조작이었다(아래 함정 참조).
     카드가 뜨는데 성가시면 체크를 눌러 끄는 흐름이 정상 동작하는지도 같이 확인
   - **벽괴물**: 대시가 줄었으니 잡힘이 늘었을 것이다. "내가 지체했다"로 납득되는가 — 억울하면
     `threat.maxLeadPx` 1000→1200 → `speedMin` 300→270 → `speedPerCycle` 40→25 순
   - **대시 화염(B28)**: 세 갈래가 따로 노는 게 폰에서 보이는가. 밋밋하면 `drawDashFx`의 `drawPlume` 인자
     (길이·반폭·벌어짐 각), 산만하면 작은 둘의 각을 줄인다
   - 계절 나무 프레임(맥 DPR2 2.4~3.9ms) · 겨울이 "추운 흐린 날"로 읽히는가 · iOS 더블탭 돋보기 — B26에서 넘어온 미판정 항목
   - 봇 재계측: `SIM=1 npx vitest run src/core/sim-threat.test.ts --disable-console-intercept`
     (**슈퍼 켠 표**를 봐야 한다 — 슈퍼 끈 표만 보면 세션을 3배 과소평가한다)
1. **효과음 추가 (사용자 지정 — 다음 작업)**. `src/sound.ts`의 기동·복구 뼈대(한줄팡 D-009~D-012 산물)는 그대로 쓰고
   **효과음 메서드는 슈팅스타 것이라 전부 교체**한다. 런타임 의존성 추가 금지 → WebAudio로 직접 합성.
   - 필요한 소리: 로프 잡기 / 릴리스 · 스윙 펌프(속도 연동) · **찬스 발동**(히트스톱과 붙는 알림) · 회전 충전(바퀴마다 음정↑)
     · 게이지 성공(대시 발사) / 실패 · 대시 중 로켓 지속음 · 벽괴물 접근 경고(거리 연동) · 사망 · 신기록
   - 심사 요건: **리워드 광고 재생 중 게임 오디오 직접 mute**(플레이북 §3) — 광고 전후로 `sound` 뮤트/복구를 반드시 태운다
   - 음소거 상태는 `jgh.v1.muted`에 이미 저장된다. 소리 추가 후 BUILD +1
2. **한줄팡 검수 요청** (다른 저장소 `toss/hanjulpang`): 더블탭 수정 테스트 배포 `01a0532c…`까지 됐고 검수 요청은 콘솔 MCP
   (`bundle_submit_review`)가 세션에 로드돼 있어야 한다 — 2026-08-30 세션엔 안 붙어 있어 못 했다. 정글훅에서 돋보기 수정을
   확인한 뒤 hanjulpang 세션에서 진행. 슈팅스타는 검수 단계라 테스트 배포(BUILD 30)만
3. 보류 연출: 벽괴물이 찬스 때 물러나는 애니메이션(지금은 위치 순간이동) · 시작 화면 데모에 벽괴물 등장 · 계절별 벽괴물 외형
4. Phase 1 잔여(효과음은 1번으로 뺐다): 점수 제출(플레이 완료 후) · 뒤로가기 확인 모달 ·
   내용설명서에 "이어하기 광고"·"회전으로 충전하는 부스트(1년에 한 번, 발동 지점은 랜덤)" 기재(D-004·D-017·D-018)
5. 검토: 매달린 동안 "재누름 시 잡힐 앵커" 링 표시 (D-006 탈출로 가시화)

## 작업 도구

- 크롬 MCP 검수: `/tools/frame.html`(393×749) + 개발 훅 `window.__jgh`(game·tuning·preset·meters·stepOnce). 탭이 hidden이면
  rAF가 멈추므로 `__jgh.stepOnce(dt)`로 틱을 돌린다. **순간이동 캡처 함정** (2026-08-30 두 번 데임): ① 매달린 몸은
  `g.body.anchor = null`로 풀고 옮겨야 한다(강체 로프가 제자리로 되돌린다) ② 순간이동으로 찬스 지점을 넘으면 찬스가 터져
  세상이 멈추므로 `tuning.sonic.chanceStepM = 1e9`으로 찬스를 아득히 밀어둔다 (B27부터, 옛 `chanceGrabMin/Max`는 없다) ③ 저장값(매뉴얼 hide 등)은 모듈 로드 때 읽으므로 localStorage를
  바꾼 뒤엔 iframe reload. 사계절은 게임 캔버스를 오프스크린 스트립에 이어 붙여 한 장으로 본다
- 봇 계측: `src/core/sim-threat.test.ts`(SIM=1) — 표가 둘이다: 위협 유무별(슈퍼 끔)과 **슈퍼 켠 종단**(`runSuperBot`,
  세션 길이 기준표). 봇은 도전 대기(pending)면 눌러 도전하고 게이지를 맞춘다. 첫 표가 `loopsToArm`을 건드리므로 되돌리는지 확인
- 폰 접속: 아이폰 핫스팟이 IPv6 전용이면 맥이 192.0.0.2를 받아 접속 불가 → 맥의 IPv6 글로벌 주소 `http://[…]:5173/`로
- 디자인 캔버스: `design/trees/gen.py`처럼 파이썬으로 보드를 생성 → `/design` 스킬로 게시. 링크는 design/README.md

## 잊지 말 것

- 설계 변경은 D-001의 "수익 역산 원칙 5"에 비춰본다
- 랜덤은 연출·배치·발동 순서에만(D-015) — 보상 확률·유상 요소 없음(D-004). 내용설명서에 랜덤 발동 기재
- 배포는 `ait:build → ait:deploy → ait:send` 한 묶음 — URL을 연락처 "정성엽"(본인 폰)에게 아이메시지로 보내야 폰에서 확인
  가능 (수신자는 tools/imessage.local.json, gitignore). 한줄팡·슈팅스타엔 ait:send가 없어 이 수신자로 osascript 직접 전송
- 게임오버 문구는 "끝" (사용자 결정). 사망 연출은 피·붉은색 없이 — GRAC 전체이용가. 벽괴물도 잉크·노란 눈뿐
- 문서의 "폭풍"(D-013 초기 표기)은 벽괴물과 같은 것 — 사용자 표현은 벽괴물
- 점수 표시는 일수지만 저장(best)·계측(score)은 미터 그대로 (D-014) — 환산 규칙이 바뀌어도 기록이 안 깨진다
- 클라우드 세션은 콘솔 인증·브라우저 없음 → 배포·실기기 확인은 로컬에서. 콘솔 MCP는 세션 시작 때 붙어 있어야 쓸 수 있다
- sound.ts 효과음 메서드는 슈팅스타 것 — 게임 구현 때 교체
- **`Rng`는 LCG라 첫 출력이 시드에 거의 선형이다** — `new Rng(seed ^ 상수)`로 파생하면 어느 시드나 같은 값이 나온다
  (2026-08-31: 찬스 지점이 12개 시드 전부 148m). 스테이지별 랜덤은 반드시 `stageSeed(runSeed, stage)`를 거친다
- **되돌릴 수 없는 토글을 주 액션 근처에 두지 않는다** — "다시 안 보기" 히트 영역이 "꾹 누르면 도전!" 바로 아래라
  도전하려던 탭이 카드를 영구히 껐고, 카드가 없으니 되돌릴 UI도 없었다 (B26 "예시 애니메이션이 안 나온다"의 원인)
- 난이도 판정은 감이 아니라 **슈퍼 켠 봇의 세션 길이**로 — 개발자는 최상위 플레이어라 "너무 쉽다"를 그대로 따르면
  B21("많이 어려워졌다")이 재현된다
